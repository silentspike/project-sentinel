namespace sentinel;

enum EventType : byte {
    PhoneRing = 0,
    PrinterBroken = 1,
    PackageDelivery = 2,
    SBahnDelay = 3,
    FireAlarmDrill = 4,
    CakeInKitchen = 5,
    AirConBroken = 6,
    InternetOutage = 7,
}

table ChaosEvent {
    event_type: EventType;
    target_room: uint16;
    target_agent: uint16;
    description: string (required);
    duration_minutes: uint32;
    timestamp: uint64;
    tick: uint64;
}

root_type ChaosEvent;
