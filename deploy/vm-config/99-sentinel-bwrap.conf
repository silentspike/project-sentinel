# Bubblewrap must be able to create an unprivileged user namespace while
# AppArmor itself remains enabled. This matches the production runtime base.
kernel.apparmor_restrict_unprivileged_userns = 0
kernel.unprivileged_userns_clone = 1
