//! sentinel-dashboard-backend entrypoint (#431). Skeleton — wiring is filled in
//! across the #431 sub-tasks (HTTP server, WebTransport endpoint, auth, proxy).

fn main() -> anyhow::Result<()> {
    Ok(())
}
