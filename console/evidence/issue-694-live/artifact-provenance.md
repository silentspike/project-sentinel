# Workbench artifact provenance

Result: PASS.

The candidate lineage contains two immutable candidate artifacts, one exact
QA workbench receipt, test evidence, an approval, and one release-manifest
evidence node. Artifact success required the expected count, digest, size,
media kind, invocation, request, agent, project, work-item, workspace, runtime,
profile, and policy bindings before the durable workflow transition. The
promoted candidate, QA run, release, delivery, acceptance, and closeout all use
the same generation-19 authority chain.
