# Artificial Duplicate Negative Gate

Pending exact GitHub run evidence. The final record will contain:

- the temporary manifest and lockfile delta;
- remote `cargo-deny` failure output;
- the failing Bans and `ci-pass` run links;
- the cleanup commit proving the fixture is absent from the final tree.
