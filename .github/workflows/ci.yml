name: CI

# Switch runner mode via GitHub repo variable:
#   Settings → Variables → Actions → RUNNER_MODE
#   "hosted"      → ubuntu-latest (uses GitHub-hosted minutes)
#   "self-hosted"  → [self-hosted, linux, x64] (default)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────
  # Detect which parts of the codebase changed
  # ──────────────────────────────────────────────
  changes:
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      go: ${{ steps.filter.outputs.go }}
      console: ${{ steps.filter.outputs.console }}
      schemas: ${{ steps.filter.outputs.schemas }}
      config: ${{ steps.filter.outputs.config }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
        id: filter
        with:
          filters: |
            rust:
              - 'crates/**'
              - 'services/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'rust-toolchain.toml'
            go:
              - 'cmd/**'
              - 'services/sentinel-judge/**'
              - 'services/sentinel-nats-bridge/**'
              - 'pkg/**'
              - 'go.work'
              - 'go.work.sum'
            console:
              - 'console/**'
            schemas:
              - 'schemas/**'
            config:
              - 'config/**'

  # ──────────────────────────────────────────────
  # Always runs - fast checks (<30s)
  # ──────────────────────────────────────────────
  lint:
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Reject hidden Unicode / Bidi characters
        run: |
          if grep -rP '[\x{200B}-\x{200F}\x{202A}-\x{202E}\x{2066}-\x{2069}]' \
            --include='*.rs' --include='*.go' --include='*.ts' --include='*.js' \
            --include='*.md' --include='*.toml' --include='*.yml' --include='*.yaml' \
            --include='*.fbs' --include='*.sql' .; then
            echo "::error::Hidden Unicode/Bidi characters found!"
            exit 1
          fi

      - name: Unsafe baseline
        run: python3 scripts/check-unsafe-baseline.py

      - name: Fenced-writer enforcement (#496)
        run: python3 scripts/check-fenced-writers.py

      - name: Projection provisioning contract (#644)
        run: bash scripts/check-projection-provisioning.sh

      - name: Runtime-base provisioning contract (#646)
        run: bash scripts/check-runtime-base-provisioning.sh

      - name: Validate TOML configs
        run: |
          for f in $(find config -name '*.toml' 2>/dev/null); do
            python3 -c "import tomllib; tomllib.load(open('$f','rb'))" || exit 1
          done

      - name: Typo detection
        uses: crate-ci/typos@631208b7aac2daa8b707f55e7331f9112b0e062d # v1.44.0

  # ──────────────────────────────────────────────
  # Rust static analysis - fmt + clippy + machete
  # Parallel to rust-doc / rust-test / rust-ebpf (fans across runners)
  # ──────────────────────────────────────────────
  rust-static:
    needs: changes
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.config == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    env:
      CARGO_BUILD_JOBS: ${{ vars.RUNNER_MODE == 'hosted' && '2' || '3' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # pinned
        with:
          toolchain: "1.97.1"
          components: rustfmt, clippy
      - name: Assert determinism toolchain (DEV-010)
        run: |
          EXPECT="1.97.1"; ACTUAL="$(rustc --version | awk '{print $2}')"
          echo "rustc: $ACTUAL (DEV-010 expects $EXPECT)"
          test "$ACTUAL" = "$EXPECT" || { echo "::error::DEV-010 determinism profile violated: rustc $ACTUAL != $EXPECT"; exit 1; }
      - name: Configure local cache (self-hosted)
        if: vars.RUNNER_MODE != 'hosted'
        run: |
          export CARGO_TARGET_DIR="$HOME/.cache/project-sentinel/cargo-target/${{ runner.name }}/target"
          mkdir -p "$CARGO_TARGET_DIR"
          echo "CARGO_TARGET_DIR=$CARGO_TARGET_DIR" >> "$GITHUB_ENV"
      - name: Rust cache (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: sentinel-hosted-static

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Unused dependencies
        uses: taiki-e/install-action@288875dd3d64326724fa6d9593062d9f8ba0b131 # v2.67.30
        with:
          tool: cargo-machete
      - run: cargo machete || echo "::warning::Unused dependencies found (non-blocking)"

  # ──────────────────────────────────────────────
  # Rust docs - rustdoc warnings (own compile, parallel)
  # ──────────────────────────────────────────────
  rust-doc:
    needs: changes
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.config == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    env:
      CARGO_BUILD_JOBS: ${{ vars.RUNNER_MODE == 'hosted' && '2' || '3' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # pinned
        with:
          toolchain: "1.97.1"
      - name: Assert determinism toolchain (DEV-010)
        run: |
          EXPECT="1.97.1"; ACTUAL="$(rustc --version | awk '{print $2}')"
          echo "rustc: $ACTUAL (DEV-010 expects $EXPECT)"
          test "$ACTUAL" = "$EXPECT" || { echo "::error::DEV-010 determinism profile violated: rustc $ACTUAL != $EXPECT"; exit 1; }
      - name: Configure local cache (self-hosted)
        if: vars.RUNNER_MODE != 'hosted'
        run: |
          export CARGO_TARGET_DIR="$HOME/.cache/project-sentinel/cargo-target/${{ runner.name }}/target"
          mkdir -p "$CARGO_TARGET_DIR"
          echo "CARGO_TARGET_DIR=$CARGO_TARGET_DIR" >> "$GITHUB_ENV"
      - name: Rust cache (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: sentinel-hosted-doc

      - name: Rustdoc warnings
        env:
          RUSTDOCFLAGS: "-D warnings"
        run: cargo doc --workspace --no-deps --document-private-items

  # ──────────────────────────────────────────────
  # Rust tests - workspace + snapshot tests (own compile, parallel)
  # ──────────────────────────────────────────────
  rust-test:
    needs: changes
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.config == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    env:
      CARGO_BUILD_JOBS: ${{ vars.RUNNER_MODE == 'hosted' && '2' || '3' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # pinned
        with:
          toolchain: "1.97.1"
      - name: Assert determinism toolchain (DEV-010)
        run: |
          EXPECT="1.97.1"; ACTUAL="$(rustc --version | awk '{print $2}')"
          echo "rustc: $ACTUAL (DEV-010 expects $EXPECT)"
          test "$ACTUAL" = "$EXPECT" || { echo "::error::DEV-010 determinism profile violated: rustc $ACTUAL != $EXPECT"; exit 1; }
      - name: Configure local cache (self-hosted)
        if: vars.RUNNER_MODE != 'hosted'
        run: |
          export CARGO_TARGET_DIR="$HOME/.cache/project-sentinel/cargo-target/${{ runner.name }}/target"
          mkdir -p "$CARGO_TARGET_DIR"
          echo "CARGO_TARGET_DIR=$CARGO_TARGET_DIR" >> "$GITHUB_ENV"
      - name: Rust cache (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: sentinel-hosted-test

      - name: Tests
        run: cargo test --workspace

      - name: Snapshot tests
        run: |
          command -v cargo-insta >/dev/null 2>&1 || cargo install cargo-insta --locked
          cargo insta test -p sentinel-ecs --test snapshot_perception --check
          cargo insta test -p sentinel-physics --test snapshot_perception --check

  # ──────────────────────────────────────────────
  # Rust eBPF feature - separate feature compile (parallel)
  # ──────────────────────────────────────────────
  rust-ebpf:
    needs: changes
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.config == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    env:
      CARGO_BUILD_JOBS: ${{ vars.RUNNER_MODE == 'hosted' && '2' || '3' }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: dtolnay/rust-toolchain@4be9e76fd7c4901c61fb841f559994984270fce7 # pinned
        with:
          toolchain: "1.97.1"
          components: clippy
      - name: Assert determinism toolchain (DEV-010)
        run: |
          EXPECT="1.97.1"; ACTUAL="$(rustc --version | awk '{print $2}')"
          echo "rustc: $ACTUAL (DEV-010 expects $EXPECT)"
          test "$ACTUAL" = "$EXPECT" || { echo "::error::DEV-010 determinism profile violated: rustc $ACTUAL != $EXPECT"; exit 1; }
      - name: Configure local cache (self-hosted)
        if: vars.RUNNER_MODE != 'hosted'
        run: |
          export CARGO_TARGET_DIR="$HOME/.cache/project-sentinel/cargo-target/${{ runner.name }}/target"
          mkdir -p "$CARGO_TARGET_DIR"
          echo "CARGO_TARGET_DIR=$CARGO_TARGET_DIR" >> "$GITHUB_ENV"
      - name: Rust cache (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: sentinel-hosted-ebpf

      # eBPF: install kernel headers on hosted, soft-fail if unavailable
      - name: Install eBPF dependencies (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq linux-headers-$(uname -r) linux-tools-common linux-tools-$(uname -r) 2>/dev/null || true

      - name: Clippy (eBPF feature)
        run: cargo clippy -p sentinel-daemon -p sentinel-ebpf --features ebpf -- -D warnings || ${{ vars.RUNNER_MODE == 'hosted' && 'echo "::warning::eBPF clippy failed (kernel headers may be missing)"' || 'exit 1' }}

      - name: Tests (eBPF feature)
        run: cargo test -p sentinel-ebpf --features ebpf || ${{ vars.RUNNER_MODE == 'hosted' && 'echo "::warning::eBPF tests failed (kernel headers may be missing)"' || 'exit 1' }}

  # ──────────────────────────────────────────────
  # Go - only when cmd/ changed
  # ──────────────────────────────────────────────
  go:
    needs: changes
    if: needs.changes.outputs.go == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-go@7a3fe6cf4cb3a834922a1244abfce67bcef6a0c5 # v6.2.0
        with:
          go-version-file: cmd/cortex-gateway/go.mod
          cache-dependency-path: cmd/cortex-gateway/go.sum

      - name: Build
        working-directory: cmd/cortex-gateway
        run: go build ./...

      - name: Vet
        working-directory: cmd/cortex-gateway
        run: go vet ./...

      - name: Tests
        working-directory: cmd/cortex-gateway
        run: go test -race -count=1 ./...

      - name: Lint
        uses: golangci/golangci-lint-action@1e7e51e771db61008b38414a730f564565cf7c20 # v9.2.0
        with:
          working-directory: cmd/cortex-gateway
          verify: false
          args: --timeout=5m
        env:
          GOLANGCI_LINT_SKIP_CONFIG_VERIFY: "true"

  # ──────────────────────────────────────────────
  # Console - only when console/ changed
  # ──────────────────────────────────────────────
  console:
    needs: changes
    if: needs.changes.outputs.console == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Self-hosted: manual Bun install (baseline for legacy CPU)
      - name: Install Bun (self-hosted)
        if: vars.RUNNER_MODE != 'hosted'
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> "$GITHUB_PATH"

      # Hosted: setup-bun action
      - name: Install Bun (hosted)
        if: vars.RUNNER_MODE == 'hosted'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install
        working-directory: console
        run: bun install --frozen-lockfile

      - name: Type check
        working-directory: console
        run: bun run typecheck

      - name: Tests
        working-directory: console
        run: bun run test

      - name: Build
        working-directory: console
        run: bun run build

  # ──────────────────────────────────────────────
  # FlatBuffer schema validation
  # ──────────────────────────────────────────────
  schemas:
    needs: changes
    if: needs.changes.outputs.schemas == 'true'
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install flatc
        run: |
          curl -LO https://github.com/google/flatbuffers/releases/latest/download/Linux.flatbuffers.binary.clang++-17.zip
          unzip Linux.flatbuffers.binary.clang++-17.zip -d /usr/local/bin/ || true
          chmod +x /usr/local/bin/flatc || true

      - name: Validate schemas
        run: |
          for f in schemas/*.fbs; do
            flatc --no-gen-all -o /tmp "$f" || exit 1
          done

  # ──────────────────────────────────────────────
  # Gate - single status check for branch protection
  # All jobs must pass (skipped counts as pass)
  # ──────────────────────────────────────────────
  ci-pass:
    if: always()
    needs: [lint, rust-static, rust-doc, rust-test, rust-ebpf, go, console, schemas]
    runs-on: ${{ vars.RUNNER_MODE == 'hosted' && 'ubuntu-latest' || fromJSON('["self-hosted", "linux", "x64"]') }}
    steps:
      - name: Evaluate results
        run: |
          results=(
            "${{ needs.lint.result }}"
            "${{ needs.rust-static.result }}"
            "${{ needs.rust-doc.result }}"
            "${{ needs.rust-test.result }}"
            "${{ needs.rust-ebpf.result }}"
            "${{ needs.go.result }}"
            "${{ needs.console.result }}"
            "${{ needs.schemas.result }}"
          )
          for r in "${results[@]}"; do
            if [[ "$r" == "failure" || "$r" == "cancelled" ]]; then
              echo "::error::Job failed with result: $r"
              exit 1
            fi
          done
          echo "All checks passed or were skipped."
