[package]
name = "sentinel-bio"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
sentinel-common = { path = "../sentinel-common" }
sentinel-ecs = { path = "../sentinel-ecs" }

[dev-dependencies]
approx = { workspace = true }
