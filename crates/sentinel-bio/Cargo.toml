[package]
name = "sentinel-bio"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
sentinel-common = { path = "../sentinel-common" }

[dev-dependencies]
approx = { workspace = true }
bolero = { workspace = true }
