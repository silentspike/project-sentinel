# Changelog

- Add deterministic solo-first collaboration admission, hard server-derived
  eligibility, smallest-team selection, evidence-correlation penalties,
  task-specific verified reliability, sparse private routes, bounded
  termination, atomic capacity reservations, exact retry responses, and
  canonical Event Store publication (#740).

- Add versioned, authority-fenced company collaboration sessions with private
  independent claims, task-local behavior mandates, digest-bound handoff
  packets, typed bounded clarification, durable dissent, exact downstream
  consumption evidence, filtered reads, and replay-safe publication through
  the canonical Event Store append and outbox boundary (#739).

- Add the store-sealed EventEnvelopeV2 append boundary, checksummed single-owner
  migration, strict compatibility readers, cross-language inference authority
  schemas, and deterministic control vectors for the single-node M1 path.

- Issue #650 M0 correction: model-swap alerts no longer infer a provider from free text or fall back to Claude. Only one explicit catalog provider directive can request a swap, and the Gateway now applies the same inventory/Gate-B activation contract to runtime provider changes as to request dispatch.

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- Require a meaningful write rate before a relative Platform Control Plane
  baseline burst can suspend an agent, while preserving the absolute safety
  threshold and suppressing duplicate actions for already suspended agents
  (#740).

- Reserve the SQLite projection writer before a batch reads its WAL snapshot,
  so short runtime reconciliation writes wait instead of crashing a full
  projection rebuild with `SQLITE_BUSY` (#740).

- Accept only the pinned Codex CLI's exact fail-closed disabled-code-mode
  prelude before a turn, while rejecting every other pre-turn error or tool
  item, explicitly disabling both code-mode feature switches, and collecting
  bounded subprocess diagnostics before classifying failures (#650).

- Accept the productive customer, agreement, and work-item actor roles and emit
  nonzero workflow ordinals in the server-redacted delivery lineage while
  rejecting invalid role/class pairs (#650).

- Allow the ordered ECS action-persist barrier up to 60 seconds to finish one
  already-running durable episode commit during shutdown. The total worst-case
  internal drain remains 239 seconds; the 260-second systemd ceiling and
  270-second acceptance-controller timeout leave explicit orchestration margin
  without delaying a normally completing shutdown (#650).

- Exercise one representative persisted mid-journey boundary during the M0
  restart gate, then complete and replay the full journey. Journey checkpoints
  remain available for targeted diagnostics without repeating the same daemon
  restart architecture after every business mutation (#650).

- Measure Event Store pressure from SQLite pages that still contain live data,
  excluding reusable freelist pages left behind by retention. A pruned store
  can now become healthy without an unsafe online `VACUUM`, while metric-query
  failures retain the conservative physical-file fallback (#650).

- Quiesce the ECS loop at the first schedule boundary after SIGTERM and keep it
  on Input-to-Persist schedules only until the provider-action drain barrier is
  armed and acknowledged. Episode projection now also yields between durable
  event commits, so a batch already in flight cannot consume the graceful-
  shutdown budget and resumes from its exact committed cursor (#650).

- Make M0 restart acceptance stop and verify the old systemd invocation before
  starting its replacement, and prioritize the final ECS persist schedule so
  provider actions cannot be hidden by a heavy shutdown tick. Service and timer
  results remain mandatory while target units use their supported state fields
  instead of a nonexistent systemd `Result` property (#650).

- Resume the M0 restart journey from a validated existing checkpoint prefix
  and expect exactly the operations replayed by that resumed runner process
  instead of rejecting its canonical replay evidence. A deep existing prefix
  remains authoritative while each earlier checkpoint validates only the
  operations that its bounded runner actually replayed (#650).

- Give an in-flight synchronous ECS post-schedule phase its own bounded drain
  budget before the final LLM action persist barrier expires, without letting
  the local action forwarder consume that budget or exceed the systemd stop
  deadline (#650).

- Drain reserved LLM provider results through the projection admission
  forwarder and one acknowledged ECS persist tick before closing the daemon's
  action receiver during single-node shutdown (#650).

- Treat loopback HTTP 503 responses as bounded M0 readiness convergence while
  keeping redirects, authorization failures, and other unexpected statuses
  fail-closed during single-node activation (#650).

- Install the exact validated M0 release manifest as rollback-protected host
  authority so a snapshot-based deployment cannot retain stale provenance
  while activating current binaries and configuration (#650).

- Keep the token-free M0 source-work observation bounded but long enough for
  restart-driven Workbench recovery and its independent gate, with a matching
  bounded controller child-process ceiling (#650).

- Give each M0 restart-journey child a bounded process budget for replaying its
  full HTTP/observe prefix while preserving the tighter per-request timeout
  inside the journey runner (#650).

- Preserve the persisted company-work plan deadline while independent QA waits
  for dispatch, while retaining the profile's relative Workbench wall-time as
  the execution limit once the sandboxed tool starts (#650).

- Separate the M0 preflight's 15-second per-probe budget from its 30-second
  controller process deadline so restart recovery can serialize a valid
  readiness result without passing an invalid timeout to the preflight.

- Keep durable company-workflow execution intents valid across one bounded
  daemon restart without relaxing per-tool Workbench wall-time limits (#650).

- Bind company provider effects to one active project reservation and
  assignment, persist that authority in schema-v3 usage events, and reject
  cost commits without the exact durable Gateway usage operation (#650).

- Give the single-node M0 restart controller an independent 370-second systemd
  job budget so one canonical 180-second stop plus one 180-second start can
  complete without weakening the bounded HTTP journey timeout, and retry only
  explicitly temporal post-restart readiness failures within a 300-second
  convergence deadline.

- Retry the bounded single-node activation preflight across systemd's narrow
  active-before-exec race, and give rollback enough time for the daemon's
  canonical 180-second durable shutdown contract.
- Fixed the single-node M0 workbench Landlock policy so the two provisioned,
  profile-bound QA helpers and their Python launcher can execute while the
  request-specific command policy continues to bind exact input arguments.
- Keep the work-item QA Workbench deadline stable across durable outbox retries
  so a completed gate can be replayed without changing its request digest.
- Record the internal, credential-free delivery rejection reason in the daemon
  journal while keeping the public company API error response redacted.
- Encode delivery-stage idempotency keys as canonical store identifiers while
  preserving their stable operation and stage binding across retries.
- Keep current non-delivery governance roles in the company model without
  forcing them into the delivery authority inventory.
- Bind independent QA execution to an exact, profile-approved command rule for
  the sealed candidate input inventory.
- Make the canonical M0 website journey produce and package a real HTML
  entrypoint with its checked script so the independent web QA profile can
  validate the promised product rather than an incomplete script artifact.
- Refresh the daemon's security, sandbox, and eBPF observations from the
  adapter-owned process after Workbench recycling so successful QA does not
  leave its agent with a stale pre-recycle PID.

- Validate completed Workbench artifacts against the same mutable per-agent
  backing used by the attested runtime. An active sentinel-fs home view no
  longer redirects the daemon's terminal manifest check to a FUSE path that
  does not own Workbench workspace or artifact state.
- Bind the automatic independent work-item gate to an exact, digest-covered
  command rule for the built-in QA helper and its staged input paths. Gate
  execution no longer fails admission before the Workbench dispatch.
- Terminalize expired independent gates as durable `gate_timed_out` work-item
  blockers. A failed project remains auditable without leaving a pending
  outbox row that degrades every later company workflow. The independent QA or
  release authority also reconciles the company work item from review to its
  matching blocked state without granting that transition to the implementer.
- Recover completed Workbench invocations through the same bounded JSONL drain
  and terminal-replay path as polling. A non-terminal poll or recovery now
  retains the durable `Executing` record instead of exposing an empty update
  that the company workflow could misclassify as an unknown outcome.
- Keep a company-workflow execution pending when a stable Workbench invocation
  is still awaiting its local dispatch response, recovery returns a typed
  retryable runtime failure, or its exact NanoRuntime handle is temporarily
  absent during supervised process recycling. Non-retryable and unclassified
  post-dispatch failures remain durable unknown outcomes, while operator logs
  expose only the public-safe runtime error code and retryability.
- Keep the operator company-event feed restricted to validated project
  snapshots. Agreement events may reference the newly created project for
  lineage, but their agreement payload is not a project snapshot.
- Permit read-only access to bwrap's private `/dev/urandom` in the Landlock
  workbench profile so atomic completion receipts can create unpredictable
  staging names after a successful tool effect. Terminal recovery now reads
  that receipt through the retained instance-fenced handle without requiring
  the already reaped child process or cgroup to remain live.
- Keep work assigned to an off-shift employee durably pending until that
  employee's exact bwrap runtime is available. The Workbench dispatcher now
  rejects this condition before reservation or runtime I/O and distinguishes
  it from a genuinely ambiguous post-dispatch outcome; its response window
  also covers a loaded adaptive ECS tick instead of falsely blocking the work.
- Route Project Manager mutations in the canonical M0 company journey through
  its authenticated agent authority instead of the operator-only command
  surface; operator reads and the explicit customer/operator negative remain
  unchanged. The corrected semantic journey uses a new durable journey ID so
  prior operation IDs cannot be mistaken for fresh acceptance evidence. A
  pre-activation blocker now returns an untouched work graph to `planning`
  after resolution, preserving the explicit manager activation gate.
- Align single-node M0 episode readiness with the durable subject-local
  projection contract: each agent frontier may trail the global scan cursor
  when no relevant event exists, while its reported lag must equal the exact
  cursor delta and future, inconsistent, blocked, or missing frontiers still
  fail closed.
- Bound Go EventStore write-lock contention with a five-second SQLite busy
  handler so NATS PubAck adoption can converge under the daemon's live writer
  load instead of immediately abandoning each publication attempt.
- Release an exact LLM request reservation after a transport-level connect
  failure proves that no Gateway dispatch occurred; timeouts and all ambiguous
  post-connect failures remain durable and fail closed.
- Treat a successful calendar-triggered Nightrun outcome as historical evidence
  across service reactivation while still requiring the periodic health-monitor
  oneshot to complete after the current timer activation.
- Queue Platform Control Plane starts for inactive systemd dependencies with
  `--no-block`, preventing daemon `ExecStartPost` readiness from deadlocking on
  units ordered after `sentinel-daemon.service`.
- Fixed the single-node action-event authority boundary: the Gateway returns
  extracted action proposals but no longer persists them as applied actions;
  the daemon remains the sole `AgentActionReceived` producer after ECS accepts
  and applies an action. Rejected action audits remain Gateway-owned.
- Serialized health-monitor auto-restarts with the M0 activation controller so
  a rollback cannot race an independent service restart or report a false
  rollback failure after reaching the intended stopped state.
- Bound the Gateway and Judge public HTTP listeners to explicit IPv4 sockets so
  the deployed process ownership and listener surface match the single-node M0
  network contract instead of exposing dual-stack wildcard sockets.
- Stop the single-node daemon before its Gateway during activation rollback so
  in-flight LLM effects can finish or record a terminal outcome instead of
  becoming unresolved after the transport disappears.
- Stop LLM admission on daemon shutdown, supervise every reserved provider
  request, and require a bounded terminal drain before the daemon may report a
  clean exit. Pending, unreserved calls are cancelled; reserved calls retain
  the full request deadline inside the matching systemd stop budget.

### Changed
- Issue #650 live-startup correction: tolerate only the attestation writer's transient zero-length regular file before consuming its nonce-bound payload, revalidate asynchronous service-health observations immediately before starting an inactive service, and prevent a full shift transition from starting after shutdown was requested. Invalid attestation file types/links/sizes remain fail closed, stale service-health observations use idempotent `systemctl start` instead of restarting an already recovered service, projection-lag restarts retain their separate authority, and the ECS shutdown path no longer begins new roster work after SIGTERM.
- Issue #650 live-preflight correction: ignore syntactically valid listeners outside the protected M0 port set before normalizing host interface suffixes, and classify absent canonical projection offsets during a running full rebuild as bounded `read_model_projection_lag`; protected-listener mismatches and malformed present offsets remain fail closed.
- Issue #650 live-activation correction: let the daemon-local systemd boot gate accept an otherwise fully healthy runtime roster while its downstream projection is not yet started only when each affected agent carries the exact `projection_present=false` and `last_repair_status=stale` pair; inconsistent healthy/stale combinations remain fail closed, and the final full-stack preflight still requires projection convergence.
- Issue #650 live-activation correction: explicitly run the existing health-monitor and nightrun one-shots during each guarded activation, bind their successful outcomes to the current timer activation rather than a future calendar trigger, and treat absent or null projection watermarks as bounded startup convergence while malformed non-null store values remain fatal.
- Issue #650 live-activation correction: retry a transient initial NanoRuntime spawn only after its adapter transaction has rolled back, while preserving any restored logical runtime state, with a strict three-attempt bound and complete error-chain logging. Exhausted bwrap failures remove the incomplete logical runtime and fail systemd readiness instead of silently reducing the active roster.
- Issue #650 live-activation correction: separate daemon-local systemd startup readiness from full-stack projection convergence. The boot gate now validates the exact healthy runtime roster while tolerating only fully quiescent projection-only rows left for the subsequently started projection worker to reconcile; any extra row carrying a PID, cgroup, security state, adapter state, or runtime resource still fails closed, and the final M0 preflight remains projection-strict.
- Issue #650 live-activation correction: reconcile unowned Sentinel cgroup-v2 directories after restoring the authoritative startup roster, terminating stale members and failing startup closed on inspection or cleanup errors so inactive-shift residue cannot permanently block daemon readiness.
- Issue #650 live-activation correction: count registry-observed live runtime PIDs instead of the retired legacy process-owner map, and require the productive episode-projection worker in daemon boot and M0 preflight readiness.
- Issue #650 live-activation correction: distinguish the bubblewrap sandbox-init Host PID reported by `--info-fd` from its nonce-attested direct `agent-runtime` child, resolve the runtime through exact `NSpid` plus PID/network-namespace identity, and move that runtime into the agent cgroup before publishing isolation readiness. This accepts the valid host/init/runtime PID translation while malformed, ambiguous, stale, foreign-namespace, or uncontained runtimes remain fail-closed.
- Issue #650 live-activation correction: traverse the Workbench completion-receipt authority from a non-reading `O_PATH` root anchor so the scoped `/artifacts` store can initialize after strict Landlock enforcement without granting global root-directory read access.
- Issue #650 live-activation correction: preserve the lightweight heartbeat runtime for ordinary ECS agent sandboxes while selecting the capability-scoped Workbench protocol only when the Landlock wrapper supplies Workbench attestation. Partial attestation remains fail-closed instead of downgrading to the general runtime.
- Issue #650 live-activation correction: require hard Landlock feature compatibility for attested workbench processes while preserving the general agent best-effort path, mask directory-only Landlock rights from regular-file rules, and make activation rollback dependency-ordered, independently bounded, and explicit about each failed unit so a valid daemon shutdown cannot strand a partially started topology.
- Issue #650 live-activation correction: keep mutable workbench input, workspace, and artifact roots on each agent's persistent writable host backing even when the normal agent home is presented through sentinel-fs, and preserve the last concrete readiness failure at a sub-100ms deadline edge.
- Issue #650 legacy episode cutover: preserve receiptless legacy memories whose historical per-agent IDs are duplicated, while continuing to reject every duplicate receipt-backed projection identity.
- Issue #650 live-activation correction: initialize the durable episode projection through an explicit, idempotent, operator-authenticated daemon preflight. Fresh stores start at the beginning, while non-empty legacy memory is sealed at the exact EventStore head without deleting or replaying existing episodes; subsequent starts validate the durable cutover receipt without requiring mutable config proofs.
- Issue #650 live-activation correction: provision a dedicated root-owned workbench authority directory before daemon startup and keep the durable invocation store there, instead of requiring ownership of the shared multi-service data root.
- Issue #650 live-activation correction: hardened systemd control-process readiness probes now read only the canonical root-owned operator credential file, avoiding the per-process credential mount that systemd does not expose to `ExecStartPost` while retaining descriptor-pinned path, ownership, mode, and content validation.
- Issue #650 live-activation correction: moved the dashboard's generated self-signed certificate state from the legacy root-owned console directory into a dedicated private runtime directory created by the canonical pre-service initializer, preserving operator-managed certificate material while allowing the unprivileged dashboard service to start without a manual ownership change.
- Issue #650 live-activation correction: added a descriptor-relative, symlink-rejecting root initializer that narrows the accepted legacy data-root mode and creates the private Gaia Console runtime and session directories, and ordered both Gaia consumers behind that canonical pre-service authority so a manifest-provisioned single-node release starts from an empty runtime overlay without manual directory creation.
- Issue #650 M0 candidate: added the authenticated product-level delivery intents and the checked-in schema-v2 single-node journey that drives a customer request through Sales qualification, governed multi-role planning, blocker escalation, isolated Designer and Developer work, sealed artifact handoff, independent QA, release, explicit customer acceptance, and durable closeout. Every mutation has stable replay semantics and a daemon-restart checkpoint; the plan, restart control, runner, preflight, and activation controller are release-manifested and fail-closed provisioned for the final `.240` acceptance.
- Issue #695 source candidate: connected the bounded company workflow to authenticated customer and agent commands, a durable SQLite workflow/event store, current organization authority, and the #694 workbench execution and completion-evidence ports. Server-owned principals, exact assignment/profile/capability checks, stable invocation IDs, output/gate contract binding before runtime I/O, bounded restart reconciliation, fail-closed dependency readiness, and joined shutdown supervision keep accepted agreements and executable work recoverable without treating chat, ticks, or agent assertions as completion. Final QA/release/delivery composition and SINGLE_NODE acceptance remain owned by #696 and #650.
- Issue #694 source candidate: added a capability-scoped agent workbench with durable digest-bound invocation records, immutable completion receipts, redacted progress and artifact events, and a fail-closed bwrap JSONL runtime path for execute, poll, cancel, and effect-free recovery. The source candidate preserves exact runtime-incarnation and current-authority checks and does not claim deployment, live acceptance, or issue completion.
- Issue #650: gated every operator-credential consumer behind one root-owned, idempotent `sentinel-auth-init` oneshot that migrates legacy authority before startup; release, provision, preflight, activation, rollback, and CI contracts now cover that unit. The stopped provisioner now performs a descriptor-checked, manifest-bounded takeover from the explicitly approved legacy numeric owner, records original metadata before mutation, and restores content plus owner/mode on failure without recursive ownership changes; `sentinel-ctl` test sockets use isolated `RUNNER_TEMP` children on hosted runners.
- Issue #650: moved daemon and dashboard operator authentication to one file-only shared credential authority, with descriptor-pinned validation and fail-closed startup when the credential is missing, ambiguous, or unsafe.
- Issue #735 source candidate: made Limbo-to-Hippocampus episode projection durable and fail closed with deterministic episode identities and source-cut-bound replay clocks, atomic episode/receipt/frontier commits, bounded retention, sealed legacy cutover receipts, restart-safe cursor reconciliation, quarantine fencing and authenticated CAS resolution. Projection integrity now survives consolidation, exposes redacted readiness diagnostics, gates affected agents at daemon memory/work boundaries while unrelated agents remain live, and supports server-rebuilt SHA-256-bound generation candidates checked against complete EventStore-authoritative source and effect material before authenticated validation, CAS activation, and retained rollback. Broad whole-product rebuild policy remains owned by #729; SINGLE_NODE live validation remains pending.
- Issue #701: added a schema-versioned, bounded, digest- and invocation-bound start/poll/cancel JSONL channel to the bwrap NanoRuntime adapter so #694 can dispatch workbench messages through the production registry. Retry-safe terminal retention, typed safe errors, strict terminal ordering, reader backpressure, direct argv handling, and process-group plus cgroup cleanup ensure malformed, foreign, duplicate-terminal, oversized, disconnected, deadline-expired, and unacknowledged-cancel exchanges fail closed without logging protocol or argument content.
- Issue #472 source candidate: routed productive daemon lifecycle calls through the shared private `NanoRuntimeRegistry` owner for ECS-native, bwrap+Landlock, and optional WASM selection with an explicit `bwrap-landlock` fallback. Exact adapter handles, health, resources, incarnation identity, and logical suspension status are now one runtime-health and reconciliation truth: intentionally `Suspended + Degraded` remains stable, while `Active + Degraded` is non-serving. Per-incarnation cleanup, restore compensation, typed suspend/resume, default bwrap recreate snapshots, and non-replaying WASM restore remain source-tested; live validation is pending. Config Apply now uses one versioned, op-id/digest-bound SQLite decision saga with a replayable filesystem participant, atomically commits its completion event/outbox with the forward decision, and restores or forward-finalizes exact world/store/projection/runtime state under a startup fence. The microVM adapter is deliberately not registered or selectable in production because the packaged guest launcher, workload-bound readiness attestation, and durable crash-recovery ownership contract are not yet implemented; productive SINGLE_NODE Firecracker support is tracked by #775 after #687 without duplicating #553/#554 migration scope. The optional bwrap CAS-manifest extension remains default-off until #548 supplies its GC-safe ownership contract.
- Issue #633: made Cargo duplicate-version policy fail closed with `multiple-versions = "deny"`, documented every exact #632 carry-forward skip with its forcing chain and removal condition, added a path-filtered Bans job to the required `ci-pass` DAG, and documented the same-PR Renovate resolution process.
- Issue #634: codified the OSS intervention ladder and dependency ownership taxonomy, added the safe replacement and upgrade-review contracts, published an active Cargo patch/fork registry without invented entries, allowlisted the four verified official Aya Git declarations with bidirectional drift enforcement, and added an always-running fail-closed CI checker covering unregistered overrides, stale rows, missing fields, expired temporary forks, Git-source drift, `[replace]`, and Cargo source replacements.
- Issue #632: implemented every bounded recommendation from the canonical dependency audit by replacing broad Tokio, Futures, Axum, and WebTransport defaults with proven feature sets, removing four unused direct service edges, and aligning Criterion dev dependencies on the workspace version. The lockfile now contains four fewer packages and two fewer duplicate names. Two implementation-time falsifications are recorded as an append-only #632 errata overlay while the closed #631 evidence remains immutable; explicit leave and investigate outcomes retain public feature-tree, duplicate-chain, grouped-gate, Renovate, and structural-register evidence.
- Issue #698: added an explicit idempotent per-workload stop lifecycle to the shared NanoRuntime contract and all four runtime adapters. Handles now carry an opaque per-incarnation identity, active duplicate workload and runtime-path aliases fail closed, bwrap and microVM retain retry ownership until fallible cleanup completes, and WASM releases tool definitions and compiled components after their final workload stops.
- Issue #75 closeout: made a confirmed full-cage breach durable on every spawn, restart, reconcile, and restore path, and hardened cleanup so both the bwrap supervisor and its sandboxed child are terminated and reaped. Deploy-VM fault injection now proves `NotIsolated` classification, health degradation, durable `AgentIsolationFailed` emission, and process cleanup; a 60-cage concurrent VM test proves pairwise-distinct namespaces without leftovers. Added reproducible deploy-VM benchmark and 30-minute soak harnesses, aligned public sandbox documentation with the loopback-only cage, and removed the obsolete bridge/veth/nftables VM setup script.
- Reframed Cluster 12 as one target architecture with three independently shippable lanes: single-node virtual-company acceptance, the N-node cluster foundation, and long-term native-host evolution. The English architecture guide now defines the complete content-addressed NodeBlueprint, Bare/Cold/Warm/Hot/Active readiness tiers, workload durability classes, ReplicaGroups and tick continuity, hardware-fenced single-survivor recovery, a native Rust plus immutable Linux Nano-Hypervisor split, topology-aware CPU/NUMA/cache placement, and an agent-operated cross-ecosystem dependency upgrade contract. The historical Rust 1.94.0 determinism result is explicitly evidence for that historical profile rather than a current toolchain pin. The separately maintained German TOGAF SSOT received the corresponding German target-vision update.
- Issue #395: added explicit organization hierarchy tiers for all 60 agents, provider-specific immutable model routing, authenticated internal caller roles, versioned hierarchy/cost telemetry with an independent projection watermark, and matching Agent Editor and Org Chart support while preserving existing model/pricing-tier history. Follow-up hardening binds hierarchy-model mappings into the immutable catalog digest, rejects stale Gate B attestations after any tier remap, reserves stable request digests and canonical agent-owner scopes before provider execution, retains completed results in a strict reservation-only durable outbox across daemon restarts, bounds unresolved terminal records with authenticated operator compaction to durable idempotency markers, claims actions fail-closed before delivery, fixes authenticated demo networking, fails closed on non-inventory providers until Gate B attestation, records the resolved model in v2 usage events, and expands the response inspector without exposing caller credentials. The live deployment correction also accepts systemd's private root-owned `0440` credential projection for non-root services while continuing to reject group-readable files anywhere outside the unit's `CREDENTIALS_DIRECTORY`.
- Issue #646: added one fail-closed runtime-base provisioner shared by single-node and cluster deployments. It pins and verifies the production Bubblewrap package/binary, keeps AppArmor enabled with the required user-namespace override, installs the canonical agent-runtime and Landlock helper plus daemon/cgroup/sysctl/base-directory contracts, explicitly creates every base-owned runtime path, and runs a disposable real sandbox under the daemon user in a Sentinel cgroup. Protected config, data, credentials, and machine identity are excluded from every mutation helper and verified by complete metadata snapshots without reading or hashing their contents.
- Issue #644: restored canonical `sentinel-projection` provisioning. The release-manifest generator now covers the worker binary and systemd unit, a fail-closed provisioner installs the projection binary/unit/target and canonical directory contract only while the daemon is stopped, and an always-on CI check prevents the binary, paths, service membership, permissions, or no-manual-database invariant from drifting again.
- Issue #631: added a public, reproducible Rust dependency reachability and structural contribution audit with full `Cargo.lock` classification from feature-resolved per-root Cargo trees, compact canonical package/edge graph sources that independently regenerate workspace membership and active-edge outputs, complete target-annotated reverse closures for all 94 duplicate versions, feature-origin evidence, release-binary bytes and cargo-bloat crate contributions, ranked recommendations for #632, and fail-closed public-evidence sanitization with token-aware remote-command authorities and URL-safe temporary-path detection; build-server timing and performance data are excluded, and no manifest, lockfile, Renovate, deployment, or runtime state is changed.
- Issue #615 live-gate correction: initialize only the invariant shared sentinel-fs root as idempotent node-local structure, while keeping every visible shared-base content mutation World-fenced and every agent layer nano-container-fenced. A fresh follower can now open an empty filesystem without claiming World or leaving rejected CAS blobs. Cluster runtime health derives its expected set from local durable residency, ignores configured nonresident projection rows, and no longer schedules ECS persistence, simulation-clock writes, control-plane cycles, shift work, periodic World snapshots, runtime-orchestrator snapshots, shutdown runtime snapshots, or episode production on a non-World-owner; periodic projection rebuilds likewise require actual drift, while explicit operator mutations still reach typed fencing rejection.
- Extended the architecture guide (both language editions) with target-architecture contracts: dedup-pipeline contract (single memory traversal, copy-after-dedup, exact RAM existence index with durable-store authority, content-based chunk identity), efficiency doctrine (co-primary resource-per-work metrics), dependency-sovereignty intervention ladder, per-traffic-class QUIC transport profiles with the control-plane resource invariant, release engineering as part of the determinism profile, and an io_uring probe/fallback contract; corrected the two-level hash model description (BLAKE3-128 chunk identity + SHA-256 object digests), stale crate/service counts, and remaining German fragments in the English edition.
- Issue #615: made Cluster-12 ownership bootstrap fail closed. Cluster members no longer synthesize self-ownership; the chef installs and replicates canonical generation-one full owner snapshots with recipient-bound local role/activation state, persistent revision/checksum conflict detection, scope-keyed saga overlays, and restart-safe readiness rebuild. Normal guards now carry and recheck the complete owner term plus local Owner/Routable state at begin and commit across SQLite, redb, and filesystem stores. Runtime residency is derived from durable authority: followers and retired/activating scopes do not spawn, while prepared targets are present as frozen ECS entities without external processes or spawn events. The existing single-node fast path remains unchanged.
- Issue #618: hardened the Cluster-12 control plane against the fail-open paths found during PR #614 review. `PeerRegistry` now atomically binds every inbound and outbound control and block-pull QUIC connection to its authenticated certificate/NodeId and actively closes all live sessions when that NodeId is revoked; post-revoke streams and reconnects fail until explicit re-authorization. If `ClusterMetaStore` is unavailable, membership remains available but the production fallback returns typed `Rejected` responses for owner/GC requests instead of acknowledging mutations through `StubHandler`. `ProvisionNode` now persists `Completed` only after the idempotent `NodeProvisioned` audit event is durable; append failure revokes and quarantines the target. Real-QUIC and provision-saga regression tests cover both connection directions, reconnect denial, re-authorization recovery, metastore degradation, and event-store failure.
- Issue #336: added a compact acronym index to the glossary and linked the first README uses of project acronyms to it.
- Updated the DEV-010 Rust toolchain contract and all effective CI version assertions from 1.95.0 to the current stable 1.97.1 release.
- Follow-up for Issue #442: kept the Sentinel Zenoh bus daemon-local with LAN multicast scouting disabled, moved cross-node liveness from the disconnected Zenoh session to explicit cert-pinned QUIC control peers, and made provisioned daemon-only cluster members explicitly monitor no absent systemd services. Membership heartbeats bypass the effect-RPC idempotency cache, use receiver-monotonic TTLs, and preserve the `Alive -> Suspect -> Dead -> Alive` lifecycle without multicast discovery. The review correction restores the full ProvisionNode contract: target-local QUIC identity generation, reciprocal peer config, durable dynamic seed authorization, certificate-to-NodeId binding at request dispatch, and completion only after the seed observes authenticated `Alive`. Provisioning now reserves its NodeId in an fsynced journal before target mutation, reuses that identity after seed restart, compensates failures at every bootstrap fence with durable failed/quarantine evidence, and clears a prior quarantine marker only after the reserved identity rejoins. Control RPC dedup is bounded by TTL and capacity, scopes keys by authenticated peer and method, binds the request digest, and rejects conflicting reuse. CAS gossip keys bind page and payload; holder advertisements must match their authenticated peer; owner mutations require the configured chef. These guarantees remain process-local for generic RPC dedup and do not claim crash-durable exactly-once semantics.
- Refreshed vulnerable or yanked transitive dependencies, pinned the reproducible RustSec audit command, and moved the Cortex Gateway to the patched Go 1.26.5 toolchain.

### Added
- Issue #693: added the versioned `web-project-v1` virtual-company work-execution contract, a machine-readable requirement-to-owner-to-test-to-evidence matrix, and an always-on fail-closed CI validator. The contract binds customer agreement, hierarchy-governed project work, isolated agent tools, immutable artifacts, independent QA/release, delivery, NMDA/Night-Run closeout, recovery, cost, and final single-node acceptance without claiming those still-unimplemented runtime outcomes as complete.
- Issue #473: added a deployable TLS strategy for `sentinel-dashboard-backend` with the unchanged Zero-Config self-signed/hash-pinning default and an opt-in Production mode using `SENTINEL_DASHBOARD_TLS_CERT`, `SENTINEL_DASHBOARD_TLS_KEY`, and optional `SENTINEL_DASHBOARD_HOSTNAME`. Provided certificates are loaded instead of regenerated, `/api/cert-hash` returns `null`, and the console WebTransport client falls back to normal CA validation without `serverCertificateHashes`. Deployment guidance now lives under `docs/deployment/`, including internal-CA trust-store import, Let's Encrypt setup, and the #474 bind/auth boundary.
- Issue #442: added the **Gaia Console loop** as `services/sentinel-gaia-loop`, separate from deterministic `sentinel-gaia` and Voice-of-Gaia simulation input. Readiness is token-free via read-only EventStore scans plus NATS `platform_analysis` subscription, while explicit Deep/Setup sessions use native Claude Code as safe-mode `claude -p` invocations with hard per-session and rolling-window budget caps, a distinct-request rate limit, one active session, idempotent retries, closed stdin, a minimal child-environment allowlist, bounded dynamic company context, Gaia-journal-only resume, process-group timeout cleanup, and raw stream-json evidence. Session data is private (0700/0600), deployment requires persistent root-only dashboard auth, and the native Claude 2.1.214 executable is installed root-owned from a repository-pinned SHA-256. Setup uses the exact `GaiaSpec` schema and deterministic `sentinel-gaia init --spec-json` dry-run path. The authenticated dashboard backend exposes `/api/gaia/*`, the Solid console adds a `GaiaConsoleView`, deploy scaffolding includes `sentinel-gaia-loop.service`, and TOGAF/architecture updates are handed off in `console/evidence/issue-442-live/TOGAF-HANDOFF.md` with no TOGAF HTML or gitignored SSOT edits in this PR.
- Issue #443: added standalone **Gaia Console Memory** as a new `crates/sentinel-gaia-memory` crate with an explicit `MIT` package license, keeping it separate from deterministic `sentinel-gaia`, `gaia_json`, Voice-of-Gaia, and the simulation `WorldSnapshot` chain. The crate provides a bi-temporal redb fact graph (valid-time + transaction-time supersede), a Markdown `gaia-memory.md` memory file, a thin confirmation-gated CLI, read-only Hippocampus integration, and read-only rehydration from immutable SQLite EventStore metadata / projection read models / Hippocampus / the memory file with `events_replayed=0`, `event_rows_loaded=0`, and `event_copy_count=0`. Backup is intentionally crate-local export/restore of `gaia_console_memory.redb` plus `gaia-memory.md` via library + CLI, with no `WorldSnapshot` field, no snapshot schema bump, and no daemon snapshot hook; periodic export scheduling remains a #442/Ops boundary. Benchmarks were run on `.241` and recorded in `docs/benchmarks/BENCHMARK-RESULTS.md`; live verification evidence is under `console/evidence/issue-443-live/`.
- Issue #428 (Agent Deep View — refs #428, part of #426 Observe/Govern): **a single-agent panel over Sentinel's REAL telemetry — a read-only CAS-FUSE filesystem browser, per-agent activity charts from the event log, and working per-agent Start/Stop (pause/resume, NOT despawn) plus a separate confirm-gated destructive remove.** The console previously showed only bio/status rows per agent; three deep capabilities the product already has were not reachable. (1) **Daemon (read-only FS browse):** two new auth-gated operator GETs `/operator/security/agent-fs` (directory listing) + `/operator/security/agent-fs-read` (file read, 1 MiB size-cap + truncation flag) navigate an agent's `sentinel-fs` `LayerManager` by inode from the agent root (inode 1; the shared base layer is not shown), exposing per-entry size/mode/mtime + dedup `refcount` (`MetadataStore::get_refcount`) and the layer-wide dedup ratio (`storage_stats`). **Strictly read-only** — no `write_file`/`mkdir` on the path; the inode-based navigation is bounded to the agent's `aggregate_id` keyspace (no path-traversal vector), and an unauthenticated GET is rejected 401/403 (`is_protected_read_path`). (2) **Daemon (per-agent lifecycle):** new `RuntimeControlCommand::Pause`/`Resume`/`Despawn` + endpoints `POST /operator/runtime/{pause,resume,despawn}`. **Stop = pause, not despawn:** `Pause` SIGSTOPs the agent's cgroup processes and calls the existing tested `RuntimeOrchestrator::pause_agent` (status → `suspended`, `AgentStatusChanged` event), keeping the ECS entity + Memory/Evolution; `Resume` SIGCONTs (new `resume_agent_cgroup_processes`) + `resume_agent` (→ `active`). The canonical status string is `"suspended"` (the `AgentStatus` enum value), which the console renders as "Paused"; `"despawned"` is reserved for the separate, confirm-gated destructive remove, which reuses `teardown_agent_full` (→ `AgentDespawned`). An invalid transition (double pause/resume) maps to a clean `409 Conflict`, never a panic. (3) **Safety — stall-suppression (load-bearing):** a SIGSTOPped agent makes 0 kernel syscalls, so the eBPF stall detector would mark it `stalled` and the platform control-plane's `agent_stall` rule would auto-`RestartAgent` it — undoing the pause. `evaluate_rules` now takes a `suspended_agents` set and skips them in rule 1; the orchestrator feeds it the `runtime_orch` Suspended handles by name each cycle. A non-tautological negative test proves a Suspended agent in `stalled_agents` (grace exceeded) produces **no** `RestartAgent` side-effect, while the unguarded control fires it. (4) **Restart-consistency:** the startup spawn loop respawns a restored-`Suspended` agent with a fresh, active process — so it re-applies SIGSTOP immediately after `start_agent_process` (active window µs–ms, before any ECS input) to keep status + OS state consistent across a daemon restart. (5) **Dashboard-backend (Rust):** five auth-gated proxies `/api/control/agent/{id}/{fs,fs/read,stop,start,remove}` (axum `Path<id>` → daemon query/body via the existing `forward(operator_auth=true)` helper). (6) **Console (SolidJS):** a new `AgentDeepView` panel — a read-only FS tree + breadcrumb (per-entry size + dedup refcount + layer dedup ratio, utf8/hex file viewer), an activity sparkline + a hand-rolled tool donut built client-side from the real `/api/events` per-agent window (`agent_action_received` → `action_type`, else `event_type`), Start/Stop buttons + a bio/status block (`suspended` → "Paused"), and the separate confirm-gated destructive remove; reached by clicking an AgentsView card (shared `selectedAgentId` signal, consume-and-clear). Tests: daemon FS-browse (listing+dedup, truncation, traversal/non-dir reject, auth-negative) + lifecycle dispatch (200/409/404/400) + the stall-suppression negative; console `agent-deep.test.ts` (FS listing/read/navigate, sparkline+donut, Stop/Start, remove-confirm, suspended→Paused, card click → selection+openPanel). Verified live on the deploy VM (gateway/judge stay inactive — no token): read-only FS browse through the dashboard proxy returns real dirents + content + dedup (94.74 %, per-file `refcount`); an unauthenticated browse → 401; Stop → `agent_live_view.status = "suspended"` (NOT despawned — entity + memory remain, process frozen `T`), Start reverses it, the same via the `/api/control/agent/{id}/{stop,start}` proxy chain; a paused agent removed → `AgentDespawned` (then reconcile-respawned); the activity sparkline + tool donut render from a real 40-event `/api/events` window (playwright, screenshot visually inspected). **Honest boundary (restart):** a paused agent's process is correctly re-SIGSTOPped after a daemon restart (proc `T`, not running), but the projection/UI status label re-seeds to "active" from the world snapshot (the ECS world has no pause concept) and re-syncs on the next pause/resume — a fix would touch the #491 restore/seed path and is out of scope. Benchmarks (deploy VM): FS browse listing p50 2.7 ms / file read p50 494 µs; pause/resume round-trip ≈ 1 tick (1 Hz, tick-synchronous command drain); dedup 94.74 %. Evidence in `console/evidence/issue-428-live/`, benchmark table in `docs/benchmarks/BENCHMARK-RESULTS.md`. Refs #426.
- Issue #498 (Distributed CAS — `BlockResolver` over the read paths — **PR3 / 4c**, Track A, Phase 4, partially addresses #498, refs #397; **on top of PR1/#597 + PR2/#601**): **the read path — a `BlockResolver` (V9) wired over every CAS/artifact read anchor, so a read of a blob or chunk this node lacks resolves on the miss (consult the block map → pull by hash → verify → durably store → retry the read), with single-flight, negative-cache, and a pull-pin. This completes #498's read side; 4a built the locator (block map) and 4b the byte path (QUIC block-pull).** (1) **`sentinel-fs::block_resolver` — the V9 resolver:** traits `BlockStore` (local existence by hash), `RemotePull` (pull a missing blob/chunk by hash — no size, the daemon resolves it from the block map), and `BlobResolve`/`ChunkResolve` (the read-path entry points). `BlockResolver` short-circuits on a local hit, else takes a **single-flight** per-key gate (N concurrent reads of the same missing block → exactly **one** pull — an 8-thread test proves 8→1), re-checks local under the gate, consults a **negative-cache** (a holder that just failed to serve a block is not re-asked for a TTL window), pulls, and on success **pull-pins** the block (a just-pulled block is GC-protected for a grace window so a concurrent sweep cannot delete it before the read completes). (2) **The four read anchors routed (the V9 read-path inventory):** **L1** `CasStore::read([u8; 32])` resolves a missing blob on the miss then retries; **B1** `ArtifactPlane::read_chunk_raw(&ChunkHash)` and **B3** `read_chunks_decompressed` (the batch path that bypasses B1 via `segments.read_batch`) each resolve absent chunks on a miss; the composites (`read_planner`, `layer.read_file`, `home_manifest`) inherit from the leaves. `ArtifactPlane::store_pulled_chunk` durably stores a verified pulled chunk (decompress → `blake3_128 == hash` else **rejected**; `segments.append` + `FS_CHUNKS` + `FS_CHUNK_REFCOUNT` in one transaction). (3) **Bypass protection (V9):** `SegmentStore::read`/`read_batch` are now `pub(crate)` — their only callers are the B1/B3 anchors, so no future direct reader can drop below the hash and silently skip the resolver; the `check-fenced-writers` gate stays green. (4) **daemon wiring (gated, Strangler S4):** `ClusterControl::blob_resolver` builds a `BlockResolver` over a `CasBlockStore` (local existence over a fresh stateless `CasStore`) + a `DaemonRemotePull` that **bridges the sync read path to the async block-pull** — it resolves the `BlockRef` size from the block map by hash (`BlockMap::find_blob_ref`), then `block_on`s `resolve_block` (pull + verify + durable store) on the FUSE/sync thread (never a tokio worker), holding a `Weak<ClusterControl>` to avoid an Arc cycle; the orchestrator injects it via `CasStore::set_resolver` **only when the control stream + the fs layer are both present** (cluster mode), so single-node prod's read path is byte-for-byte unchanged. The chunk plane is not yet in the daemon FUSE read path (Track B/#548), so only the blob resolver is injected into the daemon; chunk-pull is a tested `sentinel-fs` primitive. **2-VM live AC-6 (.241 holder ↔ .242 puller, both on the 4c daemon):** a `resolve` probe wires a real `BlockResolver` into a fresh empty `CasStore` and drives the actual `CasStore::read` API → a node-0-only blob resolves on the read miss (`content_len=52 cached_before=false pulls=1`), and a second read is a local hit with no extra pull (`pulls=1`); node-0 logs `#498 4c blob resolver wired into the CAS read path` on a real deploy. Evidence in `console/evidence/issue-498-live/resolve.md`; register `sentinel-fs-distributed-cas-pull (#498 4c)`. With 4a (locator) + 4b (byte path) + 4c (read path) the distributed-CAS read side is complete. Production-quality code, experimental cross-node-foundation scope.
- Issue #498 (Distributed CAS — pull-by-hash over QUIC — **PR2 / 4b**, Track A, Phase 4, partially addresses #498, refs #397; **stacked on PR1/#597**): **the byte path — a QUIC block-pull server/client, durable verified publish, and the daemon `resolve_block` primitive — so a node that the 4a block map says lacks a blob fetches it by hash from a peer that holds it, authenticated, hash-verified, durably cached.** (1) **Block-pull wire (`sentinel-cluster-control::block_pull`):** the control codec's 1 MiB frame cap is too small for a blob (SPIKE-498 BUILD verdict), so block bytes ride their own QUIC bidi stream — a framed `BlockPullRequest { block_ref }` (V10: **only a `BlockRef`, never a path**), then a 1-byte status + the raw encoded blob streamed until end (no per-blob cap, >1 MiB capable). The receiver **bounds** the read at `size_bytes + 1` (an encoded blob never exceeds raw + prefix) against a hostile over-long stream. (2) **`BlockPullServer` (V10):** reuses the cluster-control cert/TLS stack (`quic_server_config` + the same `peer_fingerprint` pin gate as the control server), serves a `BlockProvider` (a blob source addressed only by content hash — never a path, never a directory listing), with a per-node inflight rate limit; a miss is a typed `NOT_FOUND`, never a path leak. (3) **`BlockPullClient`:** connects cert-pinned, requests a `BlockRef`, reads the bounded response. (4) **Durable verified publish (`CasStore::store_pulled_blob`, V28):** decodes + verifies the content identity the digest covers (`SHA-256(decoded) == hash && len == size`) — a corrupt/tampered blob is **rejected, never published** — then durably publishes it (`fsync(file)` → atomic rename → `fsync(parent dir)`) before it can be advertised; `reconcile_temp` deletes incomplete `.tmp` writes at startup so a half-written blob is never mistaken for durable. (5) **daemon wiring (gated, Strangler S4):** `ClusterControl` also binds a `BlockPullServer` on the control port + 1, backed by a CAS-over-hash `BlockProvider`, and holds a client; `resolve_block(cas, block_ref)` short-circuits on a local hit, else consults the 4a block map and pulls by hash from a pinned peer's endpoint, then verifies + durably publishes — spawned **only in cluster mode**, so single-node prod is unchanged. **2-VM live (.241 holder ↔ .242 puller, cross-host over QUIC `:8086`):** AC-2 — node-1 pulls a node-0-only blob by hash → `verified+durable=true`, a second read is a local cache hit; AC-3 — a tampered pull (one byte flipped) → digest mismatch → rejected, not published; AC-5 — durable publish + pull-by-hash latency p50 ≈ 1.6 ms (200 iters cross-host, incl. a fresh QUIC connect per pull). Evidence in `console/evidence/issue-498-live/block-pull.md`; register `sentinel-fs-distributed-cas-pull (#498 4b)`. The full `BlockResolver` over all read paths + single-flight/negative-cache/pull-pin is PR3/4c. Production-quality code, experimental cross-node-foundation scope.
- Issue #498 (Distributed CAS — block map + holder gossip — **PR1 / 4a**, Track A, Phase 4, partially addresses #498, refs #397): **the distributed-CAS metadata layer — a cluster-wide block-map locator, conflict-free holder gossip over the #569 control stream, leveled anti-entropy, and the CAS holder source — so a node that lacks a blob can learn which peer holds it. This PR builds the metadata/locator layer; pull-by-hash (the byte path) is PR2/4b and the `BlockResolver` over all read paths is PR3/4c.** (1) **`sentinel-common::block_map` — `BlockMap` (V8 locator, never liveness):** the cluster directory `BlockRef → {NodeId → slot}`; it deliberately exposes no `is_live`/`is_needed`/`ref_count` (GC must never read the map as a liveness proof, #499/V8), and iteration is deterministic (`BTreeMap` over a `NodeId`/`BlockRef` total order — `NodeId`/`BlockRef` gained `Ord` — so summaries/tests never depend on `HashMap` order, #439 lesson). (2) **`HolderAdvertisement` + conflict-free merge (V16):** each `(block, node)` slot is a last-writer-wins register keyed by the monotone freshness pair `(incarnation, cas_generation)`; a `Remove` writes a **tombstone** at its freshness so a later lower-freshness `Add` cannot resurrect a dropped holder (`Remove@G` suppresses `Add<G`), and the **incarnation is the ABA guard** (a stale pre-reboot advertisement loses despite a higher generation); `prune_expired` reclaims stale ads/tombstones (bounded growth). (3) **`sentinel-common::anti_entropy` — leveled (V25), never gossip-all-ids:** `CasInventory` yields **L1** one compact `GenerationSummary` per `(namespace, chunk_profile)` (count + max-gen + an order-independent dep-free 128-bit set fingerprint), **L2** bounded cursor-paginated inventory pages (covers every block exactly once, no skip/overlap), and **L3** on-demand `held_among` reconcile. (4) **Gossip wire over the #569 control stream (transport correction):** the issue and the merged design sketch specified Zenoh gossip, but `sentinel-zenoh::pin_loopback_listen` (#525) pins the Zenoh listener to loopback (*"cross-node = QUIC ClusterControl/#569, not Zenoh"*), so Zenoh reaches no other host; the gossip rides the existing #569 QUIC control stream instead via a new `ControlRequest::AdvertiseHolders { Vec<HolderAdvertisement> }` and a `BlockMapGossipHandler` that merges inbound gossip into a shared `BlockMap` and delegates every other RPC to the #496 owner handler. **AC-4 ("no bytes over Zenoh") therefore holds stronger: Zenoh is untouched; metadata over QUIC-control, bytes over QUIC-pull in 4b.** The design doc's D3 is revised in this PR to record the supersession. (5) **`sentinel-fs::cas_holder` — the holder source:** `CasStore::list_block_refs` rebuilds the durable-blob set (decoding each blob to recover the V7 size, skipping incomplete `.tmp` writes), and `CasHolderState` mints `Add`/`Remove` (tombstone) advertisements and a bounded republish page. (6) **daemon wiring (gated, Strangler S4):** `ClusterControl::broadcast_holders` fans the gossip out to every pinned peer; `run_cas_gossip_republish` rebuilds + broadcasts in bounded pages on a 15 s cadence — spawned **only when the control stream is active** (cluster mode), so single-node prod (no `control_bind`) is unchanged. **2-VM AC-1 (live, .241/.242 over the #569 QUIC stream):** a blob placed only on node-0 → node-1's block map reaches `block_count=1`/`applied=1`, i.e. it resolves the holder `{test-node-0}` (node-0 logs `gossip delivered (peer newly applied) applied=1`). Evidence in `console/evidence/issue-498-live/`; register line `sentinel-fs-distributed-cas (#498 4a)`. Production-quality code, experimental cross-node-foundation scope.
- Issue #435 (partially addresses #435, Part A): **CI self-hosted runner switched to a purely ephemeral provisioning model (asset + runbook).** Fixes the CT-150 incident class (2026-05-30: a stuck CodeQL Go extractor left unreapable kernel zombies that blocked the runner cgroup, requiring a node reboot, plus 136G of accumulated CodeQL cruft on a persistent runner). Part A ships versioned, secret-free assets under `deploy/ci-runner/`: `install-ephemeral-runner.sh` registers the runner with `config.sh --ephemeral` (one job, then self-destruct); fetches the **tag-exact** SHA-256 via `api.github.com/repos/actions/runner/releases/tags/v${RUNNER_VERSION}` (>= 2.329.0 minimum, not `latest` and not hardcoded); keeps the runner binary cached across re-provisions (no 100MB re-download and no api call per job); takes the short-lived registration token via `RUNNER_REGISTRATION_TOKEN` (ad-hoc, ~1h) or a `RUNNER_TOKEN_CMD` host helper (default `/opt/actions-runner/get-token.sh`) so the long-lived credential never lands in the versioned file; supports `--dry-run`. `github-runner-ephemeral@.service` is the systemd template: `Restart=always` re-provisions after each job, `ExecStartPre` wipes only `_work/` plus the stale `.runner`/`.credentials` (the cached binary stays), and `HOME`/`CARGO_HOME` live inside the workdir so `ProtectSystem=strict` does not choke cargo/CodeQL jobs. Plus `deploy/ci-runner/README.md` and `docs/ci-runner-runbook.md` (incident root cause, security-honest token strategy with GitHub-App JIT recommended and a fine-grained PAT only as a warned alternative, Part B live-migration with AC-5 verify criteria, the `RUNNER_MODE=hosted` emergency fallback, and CT 150 decommission). AC-2 was verified as already satisfied (all 16 workflows carry the `RUNNER_MODE` switch, 0 naked `runs-on: [self-hosted, linux, x64]`). Part B (live migration of CT 150 on Proxmox 10.0.0.106) is tracked ops in the runbook; #435 stays open until AC-5. `shellcheck` + `systemd-analyze verify` clean; all new files are EN-ASCII.
- Issue #497 (Per-Container Snapshot/Restore — Track A, Phase 5, partially addresses #497, refs #397): **the per-container snapshot/restore primitives for a single ECS-native container, gated by a bounded migration class and fenced by the #496 owner epoch — the building blocks the #501 move saga composes (this PR builds the primitives, NOT the live cross-node move).** (1) **AC-0 gate (the real risk):** `docs/adr/ADR-0497-G4-AC0-system-matrix.md` classifies **all 16 scheduled ECS systems** against the code (11 DIRECT / 2 INDIRECT / 2+ world-level), and a new `Frozen` marker (V11) carries `Without<Frozen>` on **every one of the 11 direct per-agent-mutating systems** (plus the shared `inject_chaos_event` helper) so a frozen container is byte-stable across ticks while the global tick runs; a **negative-control test** proves an unguarded system *does* mutate a frozen agent, so the bit-stability check is load-bearing, not decorative. The two INDIRECT systems and the daemon-loop mutations are covered by the bounded class + the owner fence, not the marker — documented in the matrix. (2) **Filtered snapshot/restore:** `snapshot_agent_ecs_state` / `restore_agent_ecs_state` capture and respawn exactly one agent (others untouched, global tick unchanged), into a new `NanoContainerSnapshot` whose ECS subset **structurally cannot hold world-scope resources** (the AC-1 "no foreign state" guarantee made unrepresentable, G0); `sentinel-redb::dump_agent_tables` reads one agent's rows across the 8 per-agent tables, never a whole-table dump (AC-2). (3) **Reference-integrity (AC-3b/V12):** a new `RouteRegistry` (G-ROUTE) resolves `agent_id → node_id/owner_epoch/route_state`, so after a despawn+respawn (new `EntityId`) a holder reaches the container by `agent_id`, never a stale handle — grounded by a repo audit showing cross-agent references are already `agent_id`-keyed. (4) **Cross-store boundary (V27):** a `SnapshotCut { owner_epoch, event_cursor, cas_pin_set, inbound_cursor }` records the consistency cut (no 2PC; K1/K2/K3); path safety (V24) is already enforced by #500a's `validate_rel_path`/`openat2` and the in-transit pin reuses #492's `pin_snapshot_blobs` (both verified, not rebuilt). (5) **Bounded class — typed rejections, never silent:** `MigrationEligibility::NotMigratable(NotMigratableReason::{ActiveInbound|ScheduledWorkActive|PendingSideEffect|UnknownAgent})` + `NotSupportedForMigratedContainer` (G-EVENTHIST); `migration_eligibility` inspects the chat buffer / Gaia thoughts / `TaskState`. (6) **#489/#496 coupling:** `fenced_per_container_snapshot` takes the snapshot under the `OwnerWriteGuard` for `scope=NanoContainer(agent)` (`cut.owner_epoch` = the fence epoch; single-node is a no-op pass, 0 `StaleEpoch`), and `to_fenced_transfer` expresses it as a `FencedStateTransfer`; the whole path is behind `per_container_transfer_enabled` (**default OFF**, Strangler — single-node prod is unchanged). **2-VM AC-5 (live, .241/.242):** `NanoContainerSnapshot::state_hash` is a container-scoped (G0), order-stable (no HashMap iteration, #439 lesson) hash of the agent state; the `per_container_transfer` harness snapshots an agent on node-0, the serialized snapshot file is `scp`-copied to node-1 (the issue's "local + manual file copy" — it replaces the #569 stream and proves primitive transfer fidelity, **not** the live move) and restored, and the container state-hash is **B == A** (`1b770ee17dd5a912`). Benchmark on .241: ~1.8 KB/agent, snapshot p50 ≈ 2 µs, restore p50 ≈ 7–8 µs, size-stable (`sentinel-daemon-per-container-transfer (#497)` in the register); same-CPU-class boundary documented (AC-DX). Evidence in `console/evidence/issue-497-live/`. Production-quality code, experimental cross-node-foundation scope.
- Issue #424 (refs #424, part of #418 — Dashboard epic): **a read-only Org Chart panel that shows the company hierarchy (department → role → agent) with the model tier per node, and jumps to the Agent Editor on click.** The dashboard only had a flat `AgentsView`; the org structure (departments, roles, reporting lines) was nowhere visible. Pure console feature — no new backend code: it reads the existing `GET /api/config/agents` (#420) as a bare `AgentConfig[]`. (1) **`console/src/views/OrgChartView.tsx`:** a pure, testable `buildOrgTree(agents)` groups department → role → agent (stably sorted) from `identity.department`/`role`, with `reports_to`/`direct_reports` shown as node metadata; rendered as a recursive CSS tree (no new graph dependency); the **model tier** per node comes from `runtime.nano_runtime` shown read-only as a raw string (`null`/empty → `"—"`, not guessed — couples #395, which still owns the tier schema); 10 s poll. (2) **AC-3 navigation:** a new shared `console/src/state/selection.ts` `selectedAgentId` signal (the agent-id channel, since `openPanel` has no id param) — an org-chart click sets it and `openPanel("agent-editor")`; `AgentEditorView` (#422) gains an **additive** effect that pre-selects that agent once its configs load and then **consume-and-clears** the signal (so a stale click never overwrites a manual pick on editor re-open), non-breaking. Registered as the `org-chart` panel in the five tiling places. Live-verified on the deploy VM (console bundle only — no daemon/gateway touch, no token risk): the tree renders the real config agents grouped department→role→agent, a node click opens the Agent Editor pre-selected, and a null tier shows `"—"`. Tests: `buildOrgTree` grouping, render + tier fallback, click → `selectedAgentId` + `openPanel`, and the consume-and-clear. Evidence in `console/evidence/issue-424-live/`. Refs #418.
- Issue #427 (closes #427, part of #426): **make LLM token usage and cost visible per agent and per model tier — cache-aware, as a time series — from real provider numbers, kept in the own telemetry system (TOGAF Cluster 05b: no Prometheus server, no OTel export, no second buffer).** Costs were only aggregated per-provider in the gateway with no agent/tier breakdown and no history. (1) **Gateway (Go, `cmd/cortex-gateway/`):** the cache breakdown used to die at the `claude_code.go` fold (`inputTokens = input + cache_read + cache_creation`) before any telemetry saw it — `LLMResponse` (and the anthropic-direct `claudeUsage`) now carry `CacheRead`/`CacheCreation` separately; `PipelineResponse` gains `cache_read`/`cache_creation`/`tier`/`cost_usd` and threads them through both response-payload branches (the daemon does not know the `EffectiveModel`/cost itself, so the gateway resolves the tier — `haiku→low`/`sonnet→mid`/`opus→high`, fallback until #395 — and the per-call cost). New cache-aware per-agent/tier counters `sentinel_tokens_by_agent_total{agent_id,tier,direction}` (direction ∈ input/output/cache_read/cache_creation) + `sentinel_cost_by_agent_usd_total{agent_id,tier}`, recorded once at the single response sink for **every** call (forward/synthesis/apicp), with the agent label normalized to `AGENT-NN`; `cost_by_agent`/`tokens_by_agent` added to `/control/traffic-stats`. (2) **Event Store (1:n — the cost info lives ONCE):** new `DomainEventPayload::AgentLlmUsage { agent_id, tier, input_tokens, output_tokens, cache_read, cache_creation, cost_usd }`; the daemon's `llm_bridge` (now wired with `Arc<EventStore>`) emits it per LLM call (fresh input recovered from the folded value for counter parity, keyed by `AGENT-NN` aggregate + `request_id` correlation + a deterministic `llm_usage_<request_id>` operation_id for idempotency). It is the highest-frequency event and lands in the shared replay/retention log, so **replay-neutrality is grounded and tested**: `daemon/replay.rs::reconstruct_inputs` skips `agent_llm_usage` (projection-only, no ECS input), proven by a non-tautological bounded-replay-injection test (a clean live forward hash == a replay over a log with `AgentLlmUsage` interleaved in the range); `projection.db` is the durable long-term time-series record (event replay only reconstructs the retained `IMMUTABLE_SNAPSHOT_MS=7d` window). (3) **Projection (Rust, `sentinel-projection`):** new `CostHandler` aggregates the events into three idempotent read-models `cost_by_agent` / `cost_by_tier` / `cost_timeseries` (minute buckets, `WHERE excluded.last_event_id > …` so a replay never double-counts), keyed by the `aggregate_id` (`AGENT-NN`). (4) **Dashboard-backend (Rust):** `GET /api/cost` reads the three read-models read-only behind `require_auth`, degrading to empty arrays + `projection:"offline"` when `projection.db` is absent (no own sample buffer — the dashboard is a pure reader). (5) **Console (SolidJS):** a new `CostView` panel — cost/token tables per agent + per tier with cache-aware columns and an inline-SVG sparkline over the minute time series, 10 s poll of `/api/cost`. Live-verified on the deploy VM (token-free fake-claude with a cache-aware usage block + one real-token window): per-agent/tier `/metrics` counters with `cache_read`/`cache_creation` > 0, `apicp_enabled` toggle + `cost_by_agent`/`tokens_by_agent`, 122 `AgentLlmUsage` events in `events.db`, `GET /api/cost` served from the projection with **zero drift** vs the events (events sums == `cost_by_agent` == `cost_by_tier`, byte-exact), `ss :9090` = the daemon text exporter (no Prometheus), and a playwright run rendering the CostView (23 agent rows, cache columns, sparkline). Benchmarks: zero tick-rate drift under the append load (effective tick stays 1000 ms; the append is off the ECS critical path), full-rebuild 14 k events < 0.5 s. Evidence in `console/evidence/issue-427-live/`. Refs #397.
- Issue #429 (closes #429, part of #426): **make the Cortex Gateway's MITM synthesis rules and per-request pipeline decisions visible and controllable in the console.** The 10 deterministic synthesis rules (`internal/synthesis/rules.go::DefaultRules`) only had a global on/off and the 200-entry response-log ring buffer recorded no pipeline decision; this adds per-rule visibility/toggle plus a Request Inspector. (1) **Gateway (Go):** the synthesis `Engine` gains a mutex-guarded `ruleEnabled` map (initialized by iterating the actual rule set, so a future rule is never silently OFF) with `RuleStates()`/`SetRuleEnabled()`; the engine is a verified process-singleton, so the toggle lives there, not in `control.Config`, and resets only on restart (process-memory only, no config-apply). New control endpoints `GET /control/synthesis/rules` + `POST /control/synthesis/rules/{name}`. The response log records `decision` (`synthesize`/`apicp`/`forward`/`dropped` — **not** `queue`; the sequencing drop writes a 204 with no entry), `rule`, and `fourth_wall` (`clean`/`break`/`regenerated` from `detection.HandleFourthWall`), set at all six `writePipelineResponse` construction sites and copied at the single sink. (2) **Dashboard-backend (Rust):** auth-gated proxies `GET/POST /api/control/synthesis-rules{,/{name}}` and `GET /api/control/traffic-responses`, plus `GET /api/control/judge-alerts` which reads the latest `judge_alert_received` per agent from `events.db`. The Judge column is agent-level and joins on the **`aggregate_id` column** (`"AGENT-NN"`), **not** `payload.agent_id` (an `AgentId` newtype that serializes numeric with a lossy `AgentId(1)` fallback) — mirroring `cockpit.rs`; it degrades to `[]` when `events.db` is offline. (3) **Console (SolidJS):** a new `SynthesisView` panel — a toggle list of the 10 rules + a global toggle, and a `VirtualScroller` Request Inspector (time/agent/class/provider/decision/fourth-wall/judge) that joins judge anomalies by normalizing the gateway's numeric `agent_id` to `AGENT-%02d` (guarded: prefixed stays, non-numeric → no join); the Judge column is empty for healthy agents (anomaly-based). Verified live on the deploy VM (token-bounded gateway window): the 10 rules list + an effective `bio_bladder` toggle; the response log shows `decision=synthesize`+`rule` and `forward` with `fourth_wall=clean`; `judge-alerts` returns `AGENT-05` via the aggregate_id column; a playwright run over an SSH tunnel renders the panel (10 rules + inspector) and toggles a rule in the live UI. Persistence and the per-request `sentinel-judge` verdict are out of scope (that verdict has no per-request key — agent-level only). Refs #397.
- Issue #498 (Distributed CAS / Pull-by-Hash — grounding, refs #397): **build-free grounding for the gated #498 implementation — a tech-selection spike, a repo-wide read-path inventory, and a block-map design sketch, so PR1/2/3 (after #496 PR2) start against fixed decisions and miss no reader.** (1) **`docs/spikes/SPIKE-498-tech-selection.md`** surveys four production content-by-hash systems (IPFS/libp2p Bitswap, casync/desync, OCI Distribution, Git pack protocol), web-cited, each with the pattern adopted, the anti-pattern avoided, and a check against the 1:n block/byte principle; it then draws the honest build-vs-reuse line — **REUSE** the cluster-control QUIC transport (#569), the membership gossip pattern (#495) and `BlockRef` (#500a), but **BUILD** a new block-stream wire protocol (the reused codec's 1 MiB control-frame cap is too small for block payloads, so large/streaming frames + backpressure + hash-verify-on-receive are genuinely new, not thin glue) — and decides the chunk profile is a **system invariant** (`gear-v1` 16k/64k/256k; #498 pulls by hash and never re-chunks, so a profile change would fork the chunk space and break cross-dedup), the codec endianness (big-endian), and rules out a DHT, a reciprocity ledger, delta chains and external storage. (2) **`docs/cas/distributed-cas-read-path-inventory.md`** is a fresh repo-wide `rg` audit (sentinel-fs + sentinel-daemon + sentinel-console-plane) classifying every blob/chunk reader; it pins the **four `BlockResolver` (V9) anchor points** (`cas.read`, `read_chunk_raw`, `read_chunks_decompressed` — a separate hook because it bypasses the single-chunk path — and optionally `read_chunk_decompressed`), names the bypass surface (`segment.read`/`read_batch` are `pub fn` below the hash → a future direct caller would skip the resolver silently, so a `check-block-readers` gate must guard the segment level or it becomes `pub(crate)`), proves `metadata.rs` and the console plane clean, and lists the gaps PR3 must close (no read-verify, no single-flight, no read-pin). (3) **`docs/cas/distributed-cas-block-map-design.md`** expands G2's `HolderAdvertisement` (V16) into a full `BlockMap = HashMap<BlockRef, HashSet<NodeId>>` that mirrors the #495 membership gossip field-for-field (subject `sentinel/cluster/blockmap/{node_id}` + wildcard, ABA via boot_id/generation, receiver-monotonic clock), **decides the gossip transport** (Zenoh map gossip + an on-demand QUIC `RefQuery` point lookup, with cross-subnet Zenoh named as gated on #495 Phase 2; full-QUIC fanout recorded as the rejected alternative), and holds the line on locator-not-liveness (V8/G7 keep-on-uncertainty) and leveled anti-entropy (V25, no gossip-all-IDs). Docs-only; no code, no script, no runtime change. Refs #498, #397.
- Issue #500a (CAS-manifest FORMAT + round-trip for a bwrap home — Track A, refs #397): **replace the bwrap nano-runtime's byte-copy home snapshot with a metadata-aware CAS manifest, built and proven as an offline format/round-trip library.** The bwrap runtime used to snapshot an agent home by reading every file into a `BTreeMap<String, Vec<u8>>` — file contents only, copied as bytes, with no symlink/mode/xattr/hardlink/sparse semantics, and restored them through an unvalidated `home.join(rel)`. This change introduces: (1) **`sentinel-common::BlockRef`** — the namespaced, self-describing G2/ADR-0498 hash model (`BlockRef { namespace: Blob|Chunk|FsTrash|Manifest, algorithm, digest, size_bytes, chunk_profile, version }`) with validated constructors that make a wrong-length digest unrepresentable; serde is the canonical lossless form, `Display`/`FromStr` a documented lossy locator. #500a is its first consumer; #498 reuses it cross-crate. (2) **`sentinel-fs::home_manifest`** — `walk_home` produces a `HomeManifest` of `ManifestEntry`s (raw-byte rel-path, kind, mode, uid/gid policy, mtime, xattrs, symlink/hardlink, sparse data-extents) where regular-file content is chunked into the `ArtifactPlane` (BLAKE3-128, the canonical chunk identity — **no second SHA-256 chunk store**) and the manifest holds only `BlockRef`s plus a per-file SHA-256 integrity hash; `rehydrate` reconstructs the tree in two passes (regular tree, then hardlinks so canonical targets exist) under **V24 path safety** — lexical `validate_rel_path` (no abs / no `..`, no `canonicalize` TOCTOU) plus a write-through-symlink defense via `openat2 RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS` anchored at the destination dir fd — and never restores the raw host uid/gid. Chunks are pinned by their ingest objects (`walk_home` returns `owned_object_ids`, kept out of the serializable manifest so the snapshot payload stays deterministic) and released via `release_manifest`. (3) **`BwrapNanoRuntime` snapshot/restore are rewired** to this manifest (the adapter opens its content `ArtifactPlane` lazily, so the constructor stays infallible and the daemon/registry callers are unaffected). (4) **`sentinel-microvm::manifest`** content-addresses the two Firecracker snapshot host files into the CAS, with the RAM-page boundary documented honestly (a multi-GB guest-RAM dump barely dedups, so it is a single SHA-256 blob; live RAM-page migration is Track F/#554). Scope is honest: this is a **format/round-trip library** — it does not touch the productive daemon spawn/snapshot/migration path and makes no bwrap live-migration claim (the live integration is **#548**). Measured on the idle bench VM (i5-1235U class): 4.2–5.5× cross-file dedup on multi-chunk homes and a 37–53× marginal 1:n transfer at 90 % chunk overlap; the `gear-v1:16k/64k/256k` chunk profile is locked by a parameter sweep. Production-quality code, experimental cross-node-foundation scope.

### Changed
- Issue #696 source candidate: composed independent QA, release, delivery, and lineage with authenticated workflow authority, #694 Workbench execution, stable retry deadlines, profile-retaining poll/recovery/cancel, a digest-bound local delivery store, Limbo publication, and a bounded Console proxy; final single-node acceptance remains owned by #650.
- Repo hygiene (housekeeping, no code or runtime change): removed ~9.4 MB of stale `old-*.png` Bun-dashboard baseline screenshots under `console/evidence/issue-433-pr-{b,d}/` (the Bun dashboard itself was deleted in #469; the `new-*`/`cutover-*` screenshots, READMEs and smoke scripts stay, and the originals remain in git history), and de-duplicated/repaired `.gitignore` — dropped a stray `.cache/ >> .gitignore` redirect line and six duplicate patterns, and explicitly whitelisted the deliberately-tracked `.env.example` and `docs/screenshots/issue380-dashboard-ebpf.png`.
- Issue #486: corrected repo-internal WorldSnapshot coverage documentation so redb dump/restore is described as 12 tables including `api_patterns`, with `RoomPhysicsState` and ArtifactPlane limits called out in the TOGAF gap report; TOGAF HTML edits remain a main-session handoff.
- Issue #496 (owner-write fencing — Phase 3a, partially addresses #496): **introduce `sentinel-common::fencing` and route every `sentinel-limbo` `EventStore` writer through one fenced write entry (V3/V19) — PR1a, the behavior-preserving strangler step.** New `OwnerWriteGuard` (a typed capability carrying a `scope` + owner `epoch`, never a raw `u64`) and `StaleEpochError` are added; the `EventStore` now funnels **all 17 of its write methods** through a single private `begin_fenced_write(&OwnerWriteGuard)` choke point. The writer inventory was re-derived exhaustively because a single missed write path is a split-brain risk: it covers the direct `INSERT`/`UPDATE`/`DELETE` writers **and** the three indirect writers that mutate `sim_metadata` through the `set_sim_metadata_conn` helper (`increment_restore_generation`, `push_dead_range`, `remove_dead_range`), which a naive direct-SQL scan misses. In PR1a the fence is a no-op that accepts every guard (`OwnerWriteGuard::unfenced`, today's single-node behavior), so the full pre-existing `sentinel-limbo` test suite proves zero behavior change; a new choke-point test asserts the entry yields a working connection under both the `World` and a `NanoContainer(agent)` scope. PR2 adds the real `OwnerRegistry`, the persistent owner epoch, and the begin+commit recheck that rejects a stale writer with `StaleEpochError`; the redb store (PR1b) and the CAS-FUSE store (PR1c) follow as separate splits. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3a, partially addresses #496): **route every `sentinel-redb` `StateStore` writer through one fenced write entry — PR1b, the behavior-preserving strangler step for the redb store.** The `StateStore` now obtains its `redb` write transaction through a single private `begin_fenced_write(&OwnerWriteGuard)` choke point; **all 17 write methods** route through it (`set_agent_state`, `set_agent_states_batch`, `delete_agent_state`, `set_relationship`, `set_personality`, `set_room_state`, `set_voice_style`, `set_behavioral_notes`, `set_narrative_summary`, `increment_evolution_version`, `set_evolution_batch`, `set_agent_facts`, `set_nmda_scores`, `set_sim_hour`, `set_room_states_batch`, `replace_api_patterns_state`, `restore_all_tables`). Unlike the limbo `EventStore`, redb has no write indirection — every mutation is a `Database::begin_write()`, so the `begin_write()` grep is the exhaustive writer inventory (the table-open in `StateStore::open` is schema setup, not a runtime writer). The fence is a no-op in PR1b (accepts every `OwnerWriteGuard`, returns the same `WriteTransaction`), so the full pre-existing `sentinel-redb` test suite proves zero behavior change; a new choke-point test asserts the entry yields a usable, committable write transaction under both the `World` and a `NanoContainer(agent)` scope. The CAS-FUSE store (PR1c) is the remaining PR1 split; PR2 then activates the real epoch check across all three stores. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3a, partially addresses #496): **route every `sentinel-fs` `MetadataStore` writer through the fenced write entry and add a CI gate that permanently enforces the fence across all three stores — PR1c, the final PR1 strangler split.** The CAS-FUSE metadata store already funneled its writes through one private `begin_write()` helper (it applies the configured durability); that helper becomes `begin_fenced_write(&OwnerWriteGuard)` and **all 16 write methods** route through it (`set_inode`, `remove_inode`, `set_dirent`, `remove_dirent`, `inc_refcount`, `dec_refcount`, `set_trash_timestamp`, `restore_from_trash`, `pin_snapshot_blobs`, `unpin_snapshot_blobs`, `restore_all_tables`, `gc_trash`, `create_file`, `create_file_allocating_inode`, `remove_file`, `next_inode`). The fence is a no-op in PR1c (the durability behavior is unchanged), so the full pre-existing `sentinel-fs` test suite proves zero behavior change; a new choke-point test asserts the entry yields a usable, committable write transaction under both scopes. **New `scripts/check-fenced-writers.py` (wired into the `lint` CI job)** fails the build the moment a new in-module writer in any of the three fenced store files (`event_store.rs`, redb `lib.rs`, `metadata.rs`) acquires a raw write handle — a `redb` `begin_write()` or, for the SQLite EventStore, a write SQL statement / `set_sim_metadata_conn` call — without going through `begin_fenced_write` (whitelisting the fence method itself, constructors/bootstrap, `&Connection` free helpers, and `#[cfg(test)]`). This is the permanent backstop for the R2 split-brain risk before PR2 activates real epoch enforcement and the three fences multiply the bypass surface. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **activate real owner-epoch enforcement — PR2a, the single-node `OwnerRegistry`.** The no-op fence becomes real: a new `sentinel-common::OwnerRegistry` (a process-global `OnceLock`, like `MetricsRegistry::global()`) is the ownership authority that mints `OwnerWriteGuard`s carrying the committed owner term (`OwnerTerm { scope, owner_node, epoch }`), and every store's `begin_fenced_write` re-checks the guard against the registry (V19), rejecting a stale or non-owning write with `StaleEpochError`. `OwnerWriteGuard` loses its public `unfenced` constructor — a guard can now **only** come from the registry (`OwnerRegistry::issue`), so the type system, not a convention, is the barrier (V3). In **single-node mode the seed owns every scope**, so every write passes and the live behavior is unchanged; the daemon pins the registry to the seed's node id at startup (`init_single_node`). New types `OwnerTerm` / `LocalOwnerState` (V4) and the `FencedStore` trait (the contract the three engines share). The registry's global default owns every scope, so the 150+ existing `open` callers, tests and benches are untouched. Verified behavior-preserving: full per-crate suites green + a live single-node smoke (monotonic `last_event_id`, **0** `StaleEpochError`, simulation unchanged). The cross-node parts that only an actual handoff exercises — per-container scope tagging (G-1), commit-time recheck (the TOCTOU window), and durable `CLUSTER_OWNER` persistence — land in **PR2b** with the cooperative handoff over the #569 control stream. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **per-write owner-scope tagging (G-1) — PR2b-1a.** Each fenced write now declares its real subject scope instead of the uniform `World` PR2a shipped: an agent-keyed write is tagged `StateTransferScope::NanoContainer(agent)` (the redb `StateStore` agent-state/personality/voice/notes/facts/nmda/evolution writers from their `AgentId` param; the fs `MetadataStore` inode/dirent/file writers from their `agent_id`; the limbo `EventStore` event appends from the event's `aggregate_id`), while batch / room / CAS-refcount / snapshot / world-meta writes stay `World` = the seed/chef owner (and `aggregate_id`s without an `AGENT-` subject, e.g. `PsiBandChanged`/`ChaosTriggered`, map to `World`). New `StateTransferScope::for_agent` / `for_aggregate` helpers. **Behavior-preserving:** in single-node mode the seed owns the `World` scope *and* every `NanoContainer`, so every write still validates (0 `StaleEpochError`); the tagging only takes effect once PR2b-2 routes a container to a different owning node. Verified: per-crate suites green + the `check-fenced-writers` gate stays green + a live single-node smoke (monotonic writes, 0 `StaleEpoch`). The commit-time recheck (V19 TOCTOU) and durable `CLUSTER_OWNER` persistence follow as the next split before the PR2b-2 cooperative handoff. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **commit-time owner-term recheck (V19 TOCTOU) via the `FencedStore` trait impls — PR2b-1b.** The `FencedStore` trait (defined in PR2a) is now actually implemented for all three stores: the redb `StateStore` and the fs `MetadataStore` return a `FencedRedbWrite` / `FencedFsWrite` wrapper that holds the issued guard, delegates `open_table` to the inner `redb` write transaction, and **re-validates the owner term in `commit()`** before committing — so a write that went stale between begin and commit (a cross-node handoff committed a newer owner term while the transaction was open) is rejected at commit rather than silently committed. The limbo `EventStore` returns the connection `MutexGuard`: SQLite autocommits each statement under the held connection mutex, so there is no TOCTOU window for a single-node write and the begin re-check is the enforcement (no separate commit re-check applies). `OwnerWriteGuard::for_test` becomes `#[doc(hidden)] pub` so downstream store tests can construct a stale guard — safe because a guard grants nothing, every write re-validates it against the registry. New `commit_rechecks_owner_term_and_rejects_stale` tests (redb + fs) assert a stale guard is rejected at commit and a current one commits. **Behavior-preserving:** single-node the owner never changes mid-write, so the commit re-check always passes (0 `StaleEpochError`); verified by the full per-crate suites + a live single-node smoke (monotonic writes, 0 `StaleEpoch`). The durable `CLUSTER_OWNER` table (ADR-3) follows as PR2b-1c. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **durable owner terms in a dedicated control-plane cluster-meta store — PR2b-1c.** New `sentinel-redb` `ClusterMetaStore` (`cluster_meta.rs`): a **separate** redb database holding the committed `OwnerTerm`s in a `CLUSTER_OWNER` table (`OwnerTerm` gains `serde`). It is deliberately **not** a `FencedStore` — it is the *authority* the owner fence consults (`OwnerRegistry` validates writes against the terms here), so putting it behind the fence it authorizes would be circular; owner-term writes are control-plane operations (governed by the registry, later quorum / Track-D `OwnerMetadataLog`), not fenced data writes, and they stay out of the data-plane store's retention/GC. (The `check-fenced-writers` gate covers only the three data-store files and therefore correctly does not cover this control-plane file.) At startup the daemon re-establishes ownership from durable state: it reads the seed's `World` owner term from the meta-store, or seeds it (`World @ epoch 1`) on first start. **Behavior-preserving:** the seed owns every scope single-node, so 0 `StaleEpoch`; a restart-persistence test asserts a written term survives a store reopen, and a live single-node smoke confirms the seeded term + monotonic writes + 0 `StaleEpoch`. PR2b-2's cooperative handoff commits real per-scope terms here cross-node, completing #496. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **cross-node owner term-tracking + the real owner control handler — PR2b-2a, the foundation increment for the cooperative handoff.** The `OwnerRegistry` becomes mutable: it gains an in-memory committed-term map (`RwLock<HashMap<StateTransferScope, OwnerTerm>>`) and `commit_owner`, so a cross-node `OwnerCommit(E+1)` updates ownership and turns the old owner's guards stale — the first real cross-node reject path (V19). A **named single-node fast path (V26)** keeps the prod write path lock-free: an atomic `mode` flag is loaded `Relaxed` on every `validate`/`current_owner` and short-circuits the `RwLock`/map lookup entirely while single-node (the seed synthesizes its ownership without taking any lock — byte-for-byte the pre-cluster behavior); only after a real cross-node commit does `validate` consult the term map under the read lock. The daemon-side **`OwnerControlHandler` replaces the Phase-3a0 `StubHandler`** on cluster nodes (via the `ControlHandler` seam, so the #569 transport is untouched): an `OwnerCommit` RPC durably persists the new term to the dedicated `ClusterMetaStore` (ADR-3, durable-first authority — the persistent store is the authority, the in-memory cache follows) and then re-establishes the in-memory registry; `PrepareHandoff` / `SourceRetiredAck` are acknowledged so the 2-VM RPC path works end-to-end (the full V1 saga — durable retired-marker, ordering, epoch monotonicity — is PR2b-2b). The registry lives in `sentinel-common` and cannot depend on the redb store it is the authority for, so the handler (which sees both planes) orchestrates persist-then-apply. `StateTransferScope` gains `Hash` + a canonical `to_wire`/`from_wire` form shared verbatim with the meta-store `scope_key` (one source of truth), and `ClusterMetaStore::list_owner_terms` lets the daemon reconcile every persisted term into the registry at startup — a committed cross-node term re-enters cluster mode after a reboot (restart-durable ownership), while the seed's own `World @ epoch 1` stays on the fast path. **Behavior-preserving:** single-node the seed still owns every scope and the fast path is never left (0 `StaleEpochError`); verified by per-crate suites (an in-process two-owner test: `commit_owner` turns the old guard stale and the new owner validates; handler persist/reject tests) + the `check-fenced-writers` gate staying green + a live single-node smoke (monotonic writes, 0 `StaleEpoch`, registry never enters cluster mode without a handoff) + a V26 steady-state overhead measurement. The cooperative cross-node handoff itself (V1/V2 + `LocalOwnerState` + Chef-SPOF + the 2-VM live ACs) is PR2b-2b/2c, which close #496. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **fix the `OwnerRegistry` cluster-mode publish ordering — PR2b-2bi (load-bearing concurrency hardening before the handoff saga).** PR2b-2a's `commit_owner` flipped the `mode` flag to cluster (`Relaxed`) *before* inserting the term under the lock; on a weakly-ordered architecture a concurrent `validate`/`current_owner` could observe `mode == cluster` yet miss the just-committed term, fall back to the seed term and wrongly accept or reject a write — a silent split-brain window. It never fired in PR2b-2a (single-node never enters cluster mode) but becomes load-bearing under PR2b-2b's real cross-node handoffs. The fix is the textbook release-acquire publish: `commit_owner` inserts the term under the write lock **first**, then `mode.store(Release)`; `validate` / `current_owner` / `is_cluster_mode` load `mode` with `Acquire`, so observing cluster mode happens-after the insert and the committed term is guaranteed visible. The single-node fast path stays lock-free (an acquire load is a plain load on x86; one cheap load-acquire on weakly-ordered archs — the V26 6.54 ns is unchanged). A `loom` model test (`crates/sentinel-common/tests/loom_owner_ordering.rs`, behind the off-by-default `loom-test` feature so normal builds and CI never pull loom) model-checks the publish/observe ordering across all interleavings + weak-memory reorderings; a negative control (the old mode-before-insert order) makes it fail, proving the test has teeth. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **the cooperative cross-node ownership handoff saga — PR2b-2ii (the V1/V2/Chef-SPOF mechanism + the V4 local-retirement fence).** Builds the ownership-only handoff (moving per-container state is #497/#501): (1) **V4 local-retirement fence** — the `OwnerRegistry` gains a durable per-scope `LocalOwnerState`; `retire_local(scope, epoch)` makes `validate` reject *this node's own* writes to a handed-off scope at epoch ≤ the retired epoch, **even during a partition** that hides the chef's `OwnerCommit` to the new owner (the source can't keep writing a scope it gave up — the partition-safe half of split-brain prevention). It is gated by an atomic `has_local_retirement` flag with the same release/acquire discipline as `mode`, so the single-node fast path stays lock-free (V26). New `ClusterMetaStore` `LOCAL_OWNER` table persists it; the daemon `restore_local_retirements` at startup so a retirement survives a restart. (2) **The chef-side saga** (`handoff.rs`) drives `PrepareHandoff → durable SourceRetiredAck → OwnerCommit(E+1)` with **V1** ordering (the target only becomes owner *after* the source has durably retired — `PrepareHandoff` returns only once the source persisted its retirement, *then* the new term is committed) and **V2** safety (if the source is unreachable the saga **aborts** — no forced failover, no ownership steal; cooperative migration only). **Chef-SPOF:** the saga runs only on the seed; chef death = no new ownerships, existing owners keep writing. A `HandoffTransport` seam abstracts the #569 RPCs, unit-tested in-process with a fake wiring two registry "worlds" (the saga moves ownership, fences the source, the target owns E+1; the abort path commits nothing). (3) The owner control handler's `PrepareHandoff` is now the **real source side** — it durably retires + acks (no longer a stub). **Behavior-preserving:** single-node never triggers a handoff, never retires, so the fast path and `0 StaleEpoch` are unchanged. Verified by per-crate suites (registry fence + restart-reload, durable `LOCAL_OWNER`, the in-process saga V1/V2 tests, the handler retirement test) + the `check-fenced-writers` gate + a single-node regression smoke. The operator trigger + the live `ClusterControl` transport + the seed worker land with the 2-VM live ACs in **PR2b-2c**, which completes #496. Production-quality code, experimental cross-node foundation scope. Refs #397.
- Issue #496 (owner-write fencing — Phase 3b, partially addresses #496): **the live operator handoff trigger + the 2-VM cross-node verification — PR2b-2c (completes the owner-fencing chain).** Adds the operator entry points and the live transport that drive the PR2b-2ii saga across real nodes: (1) **`POST /operator/handoff`** (seed-only — Chef-SPOF; a member is rejected with 503) drives `run_handoff` over the **`RpcHandoffTransport`**, which applies a step locally when its alias is this node's and otherwise issues the #569 control RPC (`PrepareHandoff` / `OwnerCommit`) — the same `block_in_place(block_on(cc.rpc))` pattern as the existing control-query path. (2) **`POST /operator/owner-check`** is a diagnostic that runs a real `OwnerRegistry::validate` probe (`{is_owner, local_retired, own_write_validates, …}`, plus an optional `guard_epoch` to re-validate a previously-issued guard) so the 2-VM ACs are deterministic per-node ground-truth, not snapshots. `AppState` gains the cluster-meta store + node alias + seed flag. **Two real bugs were found and fixed by the live verification:** (a) an `OnceLock` init-order race — an early fenced write locked the global `OwnerRegistry` to its nil single-node default *before* `init_single_node` ran, leaving the seed's registry identity nil (would break handing a scope back to the seed); `init_single_node` now runs at the very top of `run()`, before any store opens (single-node behavior unchanged — the call is `[daemon.cluster]`-gated). (b) a missing seed-only gate on `/operator/handoff` (a member could have triggered the saga, a Chef-SPOF hole). **All 7 live ACs pass with ground-truth on the 2-node test cluster** (evidence in `console/evidence/issue-496-live/pr2b2c-live-evidence.md`): 1-owner/container, cross-node stale-reject (real `StaleEpochError`), V1 ordering (durable retire on the wire before commit, timestamped on both VMs), V2 partition (net cut → abort/no-steal, retired scope fenced + non-retired writable, exactly one owner after healing), TOCTOU (a guard valid at issue rejected after a cross-node epoch advance), Chef-SPOF (seed killed → existing owners keep writing, no new ownership; restart recovers exactly one owner), restart-durability; plus a single-node regression smoke confirming the init-order fix did not scratch the lock-free fast path. VM 1069 (prod) untouched. Production-quality code, experimental cross-node foundation scope. Refs #397.

### Fixed
- Issue #762: moved memory-pressure admission ahead of every shift removal, runtime/process teardown, consolidation, replacement spawn, snapshot request, and shift-predicate mutation. A blocked or restored shift now retains the prior serving roster and unchanged predicate without busy-looping; config-apply restart recovery likewise keeps its exact serving roster and canonical snapshot tick for the normal pressure-gated transition, rejecting conflicting World/ECS ticks. An admitted transition becomes pending before its first effect, and a pressure-rejected tick keeps a tick-local snapshot fence; periodic, manual, and shutdown snapshots therefore preserve the last good complete roster, while restore, config-apply, and migration commands remain queued until readiness. Logical removals are tracked separately, completion is published only after the complete replacement roster is ready and `current_shift` advances, and the post-completion forced shift anchor then captures that complete roster.
- Issue #568 (closes #568): **a provisioned member node no longer enables the Platform LLM Analyzer by default.** `ProvisionNode` renders a minimal member `daemon.toml` (`[daemon]` + `[daemon.cluster]`); every other section fell back to `DaemonConfig` serde defaults, and `platform_controlplane.llm_enabled` defaults to `true`, so the member started the LLM analyzer (repeated `gateway_url=http://localhost:8080` connect failures + log noise, and a token-bleed risk on any node with a reachable gateway). `render_daemon_toml` now emits `[daemon.platform_controlplane]` with `llm_enabled = false`; only the seed/single-node config opts in. Found during the #495 Chunk-4c live cross-node verification. Refs #397.

### Security
- Bumped the complete Go workspace toolchain baseline to 1.26.6, fixing the standard-library vulnerabilities GO-2026-6218, GO-2026-6090, GO-2026-6089, GO-2026-5972, and GO-2026-5026 reported by `govulncheck` under 1.26.5.
- Issue #571 (closes #571): **bump `quinn-proto` 0.11.14 → 0.11.15 for RUSTSEC-2026-0185** (remote memory-exhaustion / DoS, published 2026-06-22, patched in ≥0.11.15). The crate enters the tree transitively via `wtransport` (the dashboard WebTransport); lockfile-only, semver-compatible patch within the existing `^0.11`. Pre-emptive ahead of the QUIC control stream (#569) which builds on quinn. Refs #397.
- Issue #525 (refs #525): **VM exposure hygiene — bind all introspection/health endpoints to loopback and remove stale UFW allow rules.** Three deploy-VM listeners that bound wildcard are pinned to loopback, and six stale UFW `10.0.0.0/24` allow rules (for services that no longer exist or have only loopback consumers) are removed, completing the loopback-by-default posture #474 started. (1) **Daemon Prometheus exporter** (`services/sentinel-daemon`): the hardcoded `TcpListener::bind(("0.0.0.0", 9090))` is replaced by a config-driven `[daemon.metrics] bind_addr` (default `127.0.0.1:9090`, mirroring `OperatorApiConfig` — config-only, no env); `prometheus_server` takes a `bind_addr` instead of a hardcoded port. (2) **nats-bridge health** (`services/sentinel-nats-bridge`, Go): the implicit wildcard `":port"` bind is replaced by `[server] health_bind_addr` (default `127.0.0.1:<health_port>`, respecting the configured port — no hardcoded 8083; explicit override preserved). (3) **Zenoh listen endpoint** (`crates/sentinel-zenoh`): `open_session` pins the listen endpoint to `tcp/127.0.0.1:0` in both the SHM and network-fallback paths — the bus is single-node/daemon-internal (cross-node traffic is the QUIC ClusterControl stream #569, not Zenoh), so a loopback listen has zero functional risk; the pin is warn-not-fatal, so the absence of a `loopback listen pin failed` line in the daemon log plus `ss` showing the listener on `127.0.0.1` is the definitive proof it took effect (the CI smoke test only proves the bus is not broken). (4) Stale UFW rules for 8000/8080/8081/8082/8083/8222 deleted on the deploy VM; `:8222` (nats monitor) was already loopback-bound, only its stale allow rule was removed. `docs/security/threat-model.md` residual-risk section updated. No hot-path/perf impact (config/bind-only); the Zenoh SHM-primary transport is confirmed on the deploy VM.

### Fixed
- Issue #495 (ProvisionNode member bootstrap, partially addresses #495): **three defects found during the live Chunk-4c cross-node verification that prevented a provisioned node from starting.** (1) `RenderingConfig` scp'd `daemon.toml` / the systemd unit / the token-gate drop-ins **directly** into root-owned paths as the unprivileged SSH user → `scp: Permission denied`; the privileged files are now staged to a writable `/tmp` path and `sudo install`ed into place (the same pattern the binary push already used) via a new `install_text` helper. (2) The daemon `read_dir`s `config_dir/agents` on startup (an absent dir is fatal), but the saga never created it → `config/agents` is now created. (3) The systemd unit's `ReadWritePaths` lists `/opt/sentinel/fs`, which `ProtectSystem=strict` requires to exist even when the member runs without a FUSE mount (`fs_mount` defaults to `None`) → the saga now creates `/opt/sentinel/fs` too. With these, the seed bootstraps a bare VM into a **running** cluster member end-to-end (live: node-0 GenesisSeed → `ProvisionNode` → node-1 `role=Member lifecycle=Joining`, both nodes ingesting each other's heartbeats, `NodeProvisioned` event persisted, identical binary sha256 on both nodes). The happy-path unit test asserts the new staged-install command sequence + the `config/agents` creation. Refs #495, #397.

### Added
- Issue #569 (Phase 3a0 part 3, partially addresses #569): **the daemon wiring + live verification of the QUIC control stream.** The `sentinel-daemon` now starts the control stream when `[daemon.cluster].control_bind` is set (new `cluster_control` module): it **persists** the node's control cert (`NodeCertificate::load_or_generate` under `data_dir` — a stable SHA-256 fingerprint across restarts, otherwise a peer's pin breaks every reboot; key written `0600`), spawns the `ControlServer` with a `StubHandler` and the pins from `[daemon.cluster].control_peers` (new `ControlPeer` config: alias + addr + 64-hex fingerprint, exchanged out-of-band, V10), and holds a `ControlClient` for outbound RPCs. New operator endpoint `POST /operator/control/query` (`peer_alias` + `kind` `ref`/`pin` + `block_ref`) drives a cross-node RPC and returns the typed response — the live AC trigger; #496/#499 drive these RPCs internally later. `CertFingerprint::from_hex` parses the configured pins. **Live-verified on the 2-node test cluster** (`console/evidence/issue-569-live/`): cross-host RefQuery + PinQuery round-trip both directions; idempotency dedup over the wire (re-sent key → original cached reply); cert-pin reject (a peer pinned to a wrong fingerprint → `server cert … does not match pin …`, correct pin → works); cert fingerprint stable across restarts; RPC p50 ≈ 7.2 ms. Refs #569, #397.
- Issue #569 (Phase 3a0 part 2, partially addresses #569): **the QUIC control-stream transport** on top of the part-1 primitives — a real cert-pinned quinn server + client (ADR-2). New `sentinel-cluster-control::tls` builds the quinn server/client configs from a `NodeCertificate`: a rustls verifier that accepts any peer cert *identity* but **verifies the handshake signature** (the peer proves it owns the presented cert's key — a replayed public cert cannot impersonate), so trust is decided at the application layer by the SHA-256 **fingerprint pin** (V10); TLS 1.3 only; **0-RTT off** (V18 — control mutations must not be replayable). New `ControlServer` (quinn accept loop: per-connection it enforces the peer fingerprint is pinned and closes an unpinned peer, then serves bidi RPC streams, **deduplicating per `idempotency_key`** before dispatching to the `ControlHandler`) and `ControlClient` (connects, enforces the **server** pin, opens a bidi stream, sends a `ControlEnvelope`, returns the reply). An in-process integration test runs a real quinn server+client over loopback and asserts the RPC round-trip, that a re-send with the same `idempotency_key` returns the original cached reply (exactly-once over the wire), and that **both** an unpinned server (client-side) and an unpinned client (server-side) are rejected. 10 unit + 2 integration tests. Daemon wiring (spawn under `[daemon.cluster]`, node-cert persistence + peer-pin distribution) + the 2-node live ACs are part 3. Refs #569, #397. New `sentinel-cluster-control` crate: `ControlEnvelope { request_id, idempotency_key, request }` + the `ControlRequest` RPC enum (`PrepareHandoff` / `SourceRetiredAck` / `OwnerCommit` / `RefQuery` / `PinQuery` — thin skeleton payloads; the real `OwnerTerm`/`BlockRef` fields land with #496/#499) + typed `ControlResponse` (with a `Rejected` variant so an unknown/failed request is a typed reject, never a panic); a u32-BE length-prefixed frame codec (`encode_frame`/`decode_frame`, reusing the dashboard `wt.rs` format, 1 MiB cap, truncation/oversize rejected); an `IdempotencyCache` that runs a handler exactly once per `idempotency_key` and returns the cached reply on a re-send (V5/V39, in-memory — durable dedup is the ADR-3 redb concern); the `ControlHandler` seam with a deterministic `StubHandler`; and `NodeCertificate` (rcgen self-signed) + `CertFingerprint` (SHA-256) for the V10 cert-pinning a peer matches against. Pure logic, 10 unit tests. The QUIC server/client transport + daemon wiring + 2-node live ACs are part 2. Refs #569, #397.
- Issue #495 (Phase 2 ProvisionNode execution, partially addresses #495): **the `ProvisionNode` operator command + the seed-side saga driver that bootstraps a bare VM shell into a cluster node (G3).** New `OperatorProvisionCommand { pending_target_id, requested_alias, idempotency_key }` (the host/identity come from the `PendingBareNode` allowlist via `pending_target_id`, V14 — **never** from the request, so it cannot become a free remote-exec tool) and a `POST /operator/provision` endpoint (202 accepted / 503 if the node is not a seed / 400 on a malformed body). New `services/sentinel-daemon/src/provision_exec.rs` drives the recoverable `ProvisionOp` saga over an injected `ProvisionTransport` (the SSH/scp seam): confirm the out-of-band host-key pin (AC-S1, strict `known_hosts`, no TOFU), verify + push the sha256-checked binary, render `daemon.toml` + the systemd unit + the #517 token-gate drop-ins (embedded repo templates → gateway/judge/health stay boot-gated, AC-B7/AC-S3 — no secrets copied), `systemctl enable --now`, then poll `systemctl is-active` until healthy; any step error fails the op (quarantine, AC-B6). On success a `NodeProvisioned` domain event records the topology change. A dedicated seed-only worker thread drains the command **off the ECS tick** (pure infra I/O, never touches the world). `ClusterConfig` gains `pending_targets` (the allowlist) + `provision_binary_path`. The saga/render/decision logic is transport-agnostic and unit-tested (happy-path command sequence, host-key-pin-missing, transport-error→quarantine, sha mismatch, unhealthy-target, alias-injection rejection, endpoint 202/503/400/401). At this delivery point `IssuingCert` was a documented Track-A no-op; the Issue #442 follow-up above supersedes that limitation with target-local identity generation, reciprocal pins, and observed authenticated join. Refs #495, #397.
- Issue #495 (Phase 2 membership wiring, partially addresses #495): **the daemon now publishes its own heartbeat over Zenoh and tracks cluster liveness in a shared `MembershipView` (V13).** New `services/sentinel-daemon/src/cluster_membership.rs` (`run_cluster_membership`): a subscribe task ingests peer heartbeats from `sentinel/cluster/membership/*` (JSON-decoded, own `node_id` ignored) into an `Arc<Mutex<MembershipView>>`, while a fixed-cadence publish loop emits this node's `Heartbeat` and advances the liveness state machine — both off the receiver's monotonic clock (`Instant::elapsed`), never a trusted wire timestamp. Wired into the orchestrator only when `[daemon.cluster]` is present (`tokio::spawn` for the daemon's lifetime), so a single-node daemon is unchanged. Cross-node connectivity uses Zenoh's default LAN peer discovery (the test nodes share a subnet); an explicit seed `connect` endpoint is a documented robustness follow-up. The view stays **liveness only** (V2/V38) — not a voting/owner/schedulable set. Pure-logic tests (heartbeat encode/decode roundtrip, topic-includes-node-id); the live 2-node cross-node membership assertion lands with the Genesis deploy. Refs #495, #397.
- Issue #495 (Phase 2 ProvisionNode saga, partially addresses #495): **the `ProvisionNode` saga state machine + allowlist validation + token-gate template (G3).** New `sentinel_common::provision` module: `ProvisionOpState` (the recoverable saga `VerifyingTarget → PinningHostKey → PushingBinary → IssuingCert → RenderingConfig → StartingDaemon → AwaitingHealth → ObservingJoin → Completed`, plus `Failed` for rollback/quarantine, AC-B6), `ProvisionOp` (a persistent op with `op_id`/`idempotency_key`/`node_id`, idempotent `advance()`/`fail()`, ADR-3 `PROVISION_OPS`), and `validate_pending_target` — the V14 allowlist guard: a target MUST be on the `PendingBareNode` allowlist and not expired; the host/identity come from the allowlist entry, **never** from the operator request (so `ProvisionNode` cannot become a free remote-exec tool). New repo-versioned `deploy/templates/token-gate-dropin.service.conf` (#517/G3) — the single reviewable source ProvisionNode renders + checksum-verifies onto each node so LLM-touching services stay `ConditionPathExists=/etc/sentinel/allow-llm`-gated (no token bleed on boot). Pure logic (5 unit tests: happy-path saga, fail-terminal, node assignment, allowlist membership/expiry, serde roundtrip); the live execution + the `OperatorCommand::ProvisionNode` wiring are a later step. Refs #495, #397.
- Issue #495 (Phase 2 membership model, partially addresses #495): **lightweight heartbeat-based cluster membership view (V13).** New `sentinel_common::membership` module: `Heartbeat` (the Zenoh wire message carrying the `boot_id`/`incarnation` ABA guards, no trusted wall-clock), `NodeMembership` record, `MembershipState` (`Alive → Suspect → Dead`, plus an explicit `Left`), and `MembershipView` — the chef's tracker that `ingest`s heartbeats and runs the liveness state machine off the **receiver's** monotonic clock (passed in as `now_ms`, so it is deterministic + unit-tested). ABA handling: a stale incarnation within a boot is rejected, a new `boot_id` is treated as a restart; the cross-boot delayed-packet edge is documented as Track-D hardening (boot epoch / SWIM refutation). **Membership reports liveness only** — `alive()` is explicitly *not* a schedulable/voting set (V2/V38); ownership and `VotingConfig` are separate (Track D). N-node-native (`NodeId`-keyed); the Zenoh publish/subscribe wiring + the live 2-node cross-node test follow with the Genesis deploy. Refs #495, #397.
- Issue #495 (Phase 2 foundation, partially addresses #495): **cluster node identity + lifecycle types + optional `[daemon.cluster]` config.** The N-node-native foundation for Genesis (`docs/adr/ADR-0495-G-GENESIS-…`) and ProvisionNode (`ADR-0495-G3-…`): new `sentinel_common::cluster` module with `NodeId` (Uuid, V15 — not the legacy `u16` agent alias), `NodeIdentity` (node_id + alias + cert_fingerprint + a per-boot `boot_id` + `incarnation`, the ABA guard), `NodeLifecycleState` (`PendingBare → … → Active`, plus `GenesisSeed`/`Cordoned`/`Draining`/`Quarantined` for G-N0/G-D2), `ClusterRole`, `ClusterConfig`, and `PendingBareNode` (the V14 allowlist target). The daemon gains an optional `[daemon.cluster]` config section (`#[serde(default)]` → **absent = single-node, behavior unchanged**); on startup it logs its cluster identity + role + initial lifecycle (seed → `GenesisSeed`, member → `Joining`) or "single-node mode". Backward-compatible, N-node-native (NodeId-keyed). Identity/config only — Zenoh membership, the owner registry and the `ProvisionNode` operator command build on this in later steps. Refs #495, #397.
- TOGAF Cluster 12 (target architecture): **Architecture Decision Records introduced** under `docs/adr/` (new `README.md` convention + status lifecycle + index, and `ADR-TEMPLATE.md`). The Gate-#0 ADRs fix the load-bearing cross-node forks **before** any feature code so the implementation issues build against fixed decisions: **G0** cross-node simulation time — per-node sub-worlds + async causal messaging + per-node determinism (not a global barrier tick), grounded on the global whole-world tick (`orchestrator.rs:4247`) and the `RELATIONSHIPS`/`ROOM_STATE` redb keying; **G-GENESIS** first-seed bootstrap — exactly one permitted manual deploy, nodes 1..N only via operator-approved self-provisioning from a bare VM shell, VM 1069 never a cluster member; **G-D0** `OwnerMetadataLog` — quorum-backed owner/voting metadata only (no Raft for agent state per TOGAF; witnesses are voters, two-phase `Cold→Cold+new→Cnew` voting-config change); **ADR-2** one QUIC control stream reusing the dashboard WebTransport frame codec (`wt.rs`; SSH only for the bare-shell bootstrap, no 0-RTT for control); **ADR-3** cluster metadata in dedicated redb tables (`CLUSTER_OWNER`/`MIGRATION_OPS`/…) behind the fenced write entry. Each Track-A ADR carries the N-node-native rule. Docs-only; no code or runtime change. Refs #397.
- TOGAF Cluster 12 (target architecture): **ADR gate matrix** added under `docs/adr/` — the per-gate decisions each Cluster-12 feature issue blocks on. Track-A feature gates: **G1** ownership/handoff safety (typed `OwnerWriteGuard` + one fenced write entry per engine + V1 durable-ack handoff, grounded on the ~9 EventStore / 19 redb / 18 FS write sites); **G2** CAS `BlockRef`/hash model (namespaced ref over the SHA-256 `CasStore` vs BLAKE3-128 chunker split; block map = locator not liveness; durable fsync-publish); **G3** ProvisionNode threat model (allowlist targets, out-of-band host-key pin via guest-agent, target-local private keys and reciprocal NodeId/certificate pins, repo-templated token-gates); **G4** snapshot consistency (`Frozen` over the classified mutating-system set + `SnapshotCut` + reference integrity); **G5** migration saga (persistent recoverable `MigrationOp` + epoch-correct rollback + pin lifecycle); **G7/G-DELETE** `ClusterDeleteGuard` over the full ≥11-path delete inventory (keep on uncertainty). Track-D/H gates (block forced-failover / GA, not Track A): **G6+G8** `RecoveryPoint` + RPO/durability classes, **G9** binary provenance, **G-H2** cold backup/restore, **G-N0** N-node control-plane object model, **G-D2** node lifecycle, **G-D3** CAS replication/repair. Docs-only; no code or runtime change. Refs #397.
- Issue #494 (Phase 1, partially addresses #494): **determinism profile (DEV-010) made a CI-enforced operating condition.** For TM-3 restore-via-replay (#491) and homogeneous cross-node migration (#501) to reproduce a state hash bit-/state-identically, "same CPU architecture" is not enough (Rust `f32::exp/sin/ln/sinh` are "unspecified precision"); instead of the fixed-point rewrite (#406) a strict determinism profile is the operating condition. New `docs/togaf-deviations-v22.md` **DEV-010** records the 6-point profile (identical CPU/masked features · identical binary/Rust 1.94.0/flags · no fast-math/FMA contraction · same libm/std · fixed rounding/subnormal policy · deterministic iteration order) + the homogeneous-only boundary (#394/#406) + the **G0** note that cross-node migration is **state transfer, not recompute** (so cross-CPU-class recovery is correct without f32 identity — the equal-hash check across nodes is a same-class verification aid, never the migration gate). New CI `#[test] determinism_two_runs_identical_hash` in `crates/sentinel-ecs/src/hash.rs` runs two identical bio+ECS tick sequences (10 agents × 50 ticks) and asserts an identical STRICT+CORE `sentinel_ecs::hash::state_hashes` — the intra-run determinism gate (catches HashMap/iteration nondeterminism; cross-ISA float drift is the documented homogeneous-only boundary, not test-catchable on one machine). All four CI Rust jobs now pin `toolchain: "1.94.0"` and assert the **effective** `rustc --version == 1.94.0` (the `dtolnay/rust-toolchain` action was pinned to a commit tagged `# stable`, not a literal channel — the action ref alone was not proof the determinism toolchain is in effect). New `scripts/check-determinism-profile.sh` verifies the pinned toolchain + the absence of fast-math/FMA/target-feature overrides; `deploy/deploy-preflight.sh` documents the "identical binary + toolchain on all nodes" invariant. Single-node correctness gate, homogeneous-only — no cross-node or "ms" claim.
- Issue #421 (closes #421): **Gaia Web Wizard** — create a company in the browser end-to-end (Epic #418 Configure/Build). New `GaiaWizardView` (SolidJS, registered as a tiling panel) drives a 6-step flow (company / shift / departments / culture / preview / success) over a `createStore<GaiaSpec>` with snake_case enums mirroring `sentinel-gaia`; each step's "next" is gated by a pure `validateGaiaStep` in the new `views/config/validation.ts` (a client mirror of the Rust validators — header comment marks the server `validate_apply` as authoritative SSOT). The preview step calls the new **server-side** `POST /api/config/generate` (deterministic `sentinel_gaia::generate`, blake3, no LLM) which parses the generated `agents/*.toml` + `rooms.toml` into `{summary{agent_count,room_count,shift_distribution}, agents, building}` and shows it **without persisting**; the final deploy posts `mode:"fresh"` to `/api/config/apply` behind an explicit overwrite checkbox (destructive — gated).
- Issue #422 (closes #422): **Agent Editor** — edit an existing agent's TOML in the UI. `AgentEditorView` loads `/api/config/agents`, populates a `createStore<AgentConfig>` on selection (Big Five + caffeine sliders `[0..1]`, `morning_person` checkbox, preferences/background/capabilities text, `nano_runtime` shown **read-only** until #395 supplies a tier schema), highlights a dirty diff against the loaded state, and saves via the new **`PUT /api/config/agents/{id}`** (backend load-all → replace by id (404 if unknown) → `mode:Live` non-destructive apply-proxy to the daemon, the sole `config_dir` writer per #420/#474). Save is gated by `validatePersonality` + `validateAgentRequired` (empty name → inline error, save disabled).
- Issue #423 (closes #423): **Config Editor** — two tabs. **Rooms** is fully editable with an **auto-mirrored adjacency** toggle (checking a↔b updates `adjacent` on both rooms) so the one-sided adjacency that `BuildingConfig::validate` rejects can never be created from the UI; `validateAdjacency` is a client safety net and saves go via the new **`PUT /api/config/rooms`**. The **daemon** tab is a deliberate **read-only viewer** of `max_agents`/`time_scale`/`tick_rate_ms` (parsed server-side, `GET /api/config/daemon` extended to return the parsed whitelist alongside the raw text) — `OperatorConfigApplyCommand` carries only `agents`+`building`, and the net-exposed dashboard must never write `config_dir` (#420/#474), so editing those runtime params is a tracked follow-up needing a daemon operator endpoint (not a `scope:partial` — rooms is 100% editable, daemon-params is out of scope by design). Shared foundation for all three: `sentinel-gaia` added as a backend lib dependency, the three new endpoints registered under the existing auth-gated `/api/config` router, `putJson` added to the console API, and three new tiling panels.

 so a restore to a retained snapshot always yields complete, readable agent files (no later FUSE `EIO`). A restore restores the FS metadata (inode→hash map), but the snapshot carries no blob bytes; if Trash GC had deleted a referenced blob in the meantime the inode tree pointed into the void and the *read* failed (not the restore). New redb table `FS_SNAPSHOT_BLOB_REFS (snapshot_id, hash) → pinned_at` is an explicit pin manifest derived from the **inode** hashes a snapshot actually references (`referenced_blob_hashes`: `FileKind::Regular` inode hashes, NOT the `refcounts` aggregate — a retention manifest must not depend on live-fs state); `SnapshotManager::create_and_store` pins on snapshot create, `delete_redundant` unpins on retention removal (`maintain()` threads the fs layer through). `gc_trash` skips any blob pinned by a retained snapshot even at refcount 0 (a transient `pinned_hashes()` HashSet built once per pass, O(1) membership). V1 snapshots (`fs_metadata: None`) create no pins → unpin is a safe no-op (best-effort restore). The existing `validate_fs_metadata_blobs` stays as the pre-commit backstop (AC-5). The pin is a **pointer**, never a blob copy (1:n CAS). New tests (pin blocks GC then unpin frees, pin survives a partial unpin, V1 no-op, inode-hash extraction). New standalone benchmark `crates/sentinel-fs/benches/snapshot_pin_gc.rs` (deploy-VM i7-3930K, daemon active): the pin-index scan (`pinned_hashes()`) is **linear at ~0.27 µs/pin** (19/209/1344 µs at 50/500/5000 pins) — confirming one `(snapshot_id, hash)` table + transient HashSet scales (no secondary hash-first index needed); the larger `gc_trash` pass (1.4/14.5/154 ms) is CAS-unlink-IO-dominated; bug-finder green (no pinned blob ever deleted across the 100/1k/10k sweep). No external stack (TOGAF Cluster 11/12 is target architecture).
- Issue #493 (TM-5, closes #493): **dead-branch GC** — after a restore jumps back in time, the discarded "future" (events after the anchor) becomes overwritable/deletable instead of lingering. `events` stays the untouched append-only SSOT (no per-row generation column, no migration, no hot-append tagging — `events.id` AUTOINCREMENT already makes a discarded interval unambiguous; Maintainer decision 2026-06-06: simple epoch first, not a full `branch_id`). A new `sim_metadata(key, value, updated_at)` side table holds `restore_generation` (monotonic) + `dead_ranges` (JSON `(from_exclusive, to_inclusive]` id-intervals); `commit_world_restore_stores` marks the discarded future on every restore (`increment_restore_generation` + `push_dead_range(anchor, head)`; anchor = the replay target cursor or the snapshot's `last_event_id`; rollback does not mark). ONE shared `dead_range_exclusion` guard is applied uniformly across **every** event-read path (`get_events_since`/`_range`/`_since_with_id`/`_by_aggregate`/`_by_correlation`/`get_all_events`/`count_events_in_range` + `poll_outbox` via an `events` join on `e.id`), so a discarded future is never served, published, or replayed — a per-read-path test goes red if one is forgotten. `can_prune` excludes dead-event outbox entries so a discarded future's stuck-pending entries cannot deadlock pruning permanently; `prune_batch` additionally deletes dead-interval events (even above the retention cutoff) and clears a `dead_ranges` entry once its interval is empty (the list stays bounded). The dashboard backend's console event feed (events route, live stream, recent + focus backfill) consults the same `dead_ranges` SSOT via `dead_range_filter`. Couples #492 (a dead generation stops pinning once its snapshots are retention-removed) and #491 (the bounded-replay input): a daemon-level STRICT/CORE hash-chain test proves a replay window crossing a dead boundary reproduces the live timeline exactly with dead events never contributing — the most dangerous Time-Machine break if it regressed. New standalone benchmark `crates/sentinel-limbo/benches/dead_branch_gc.rs` (deploy-VM, daemon active): the read-guard overhead is within measurement noise at the realistic small `k` (the prune-clears keep `dead_ranges` short) and ~0.6 ms only at 64 accumulated intervals; prune throughput 2131/4188/9286 events/s at 1k/10k/100k dead events with the bug-finder green (exactly the dead events removed, the 1000 live survive, `dead_ranges` cleared). No external stack (Cluster 11/12 target architecture).
- Issue #529 (TM-3 follow-up, closes #529): **exact bounded replay across nightrun/shift boundaries via snapshot-on-shift-transition.** AC-1 characterization (`docs/spikes/SPIKE-529-cross-nightrun.md`, measured, 0-token) pinned the cross-shift divergence to the **active agent set** (CORE-level), with two compounding causes: R1 — `run_bounded_replay` runs only the ECS schedule, while the shift transition (despawn/respawn) is daemon-loop orchestration (`RuntimeOrchestrator::shift_transition`), not an ECS system, so a replay across a shift never reproduces it; R2 — `detect_current_shift()` is wall-clock at `time_scale == 1.0`, so the live shift fires at a non-deterministic tick. (Not the evolution result, which is redb-only and outside the ECS hash.) Rather than rebuilding the shift inside the replay (surgery on the #491 core path), the daemon now **forces an anchor snapshot immediately after every shift transition** (`SnapshotManager::mark_shift_snapshot_pending` → the periodic snapshot block captures the post-shift state at the shift tick; interval behavior otherwise unchanged). Then for any target ≥ the shift tick the nearest anchor ≤ target is the post-shift snapshot, so the replay window `(anchor, target]` is always **within a single shift** — where #491's engine is already byte-exact — and never crosses a boundary. `select_anchor_snapshot` now accepts `tick <= target_tick` (a snapshot exactly at the target = empty replay = exact post-shift state, covering `target == shift_tick`; the #528 guard rejecting `tick > target` is preserved). Additive, no change to the #491 replay/restore core; couples to #250 Tiered Retention. The R1 measurement is kept as a permanent invariant test (`replay_does_not_reproduce_shift_transition_r1`) documenting why "never replay across a shift" is the correct contract. Satisfies the TOGAF Cluster 11 mechanism (nearest anchor ≤ target + bounded replay) literally by keeping the window in scope.
- Issue #491 (TM-3, closes #491): **arbitrary tick/event restore via anchor snapshot + bounded deterministic replay** — the production Time-Machine slider for the first hour. `POST /operator/restore` now takes exactly one of `{snapshot_id}` (direct snapshot point, unchanged), `{target_tick}`, or `{target_event_id}` (`OperatorRestoreCommand::validate`; old `{snapshot_id}`-only JSON still deserializes). For a tick/event target the daemon resolves the youngest anchor snapshot `≤ target`, restores it, then deterministically replays the event range `(anchor, target]` on the live world: agent actions + operator commands (chaos/stimulus/gaia/broadcast/dm) and the PSI band are reconstructed from the event log (`source == "autonomy"` actions are skipped — the autonomy system regenerates them), and the projection is seeded from the **post-replay** state. The replay runs through the existing `FencedStateTransfer` (now carrying a `target_cursor`) under the restore fence, with side effects gated by a RAII guard that swaps the perception/action/operator channels to scratch sinks and removes the Limbo/Zenoh/redb resources (no event re-append, LLM, or NATS during replay). Honesty guards: replay only runs when the anchor is schema v3 (post-PR-A); a `snapshot_restored` event inside the range (non-linear history) or a span over the `14 400`-tick cap falls back to the nearest snapshot with `exact:false`; the `snapshot_restored` audit event records `anchor_snapshot_id`/`target_event_id`/`exact`/`replayed_inputs`. New read-only `GET /operator/state-hash` (auth-gated, via the tick-loop command pattern) returns the canonical `{strict, core, tick, last_event_id}` taken at end-of-tick — the same cycle position as the post-replay hash — so `live@T` and `restore(@T)+replay` are directly comparable. The in-process integration test `replay::tests::live_equals_restore_plus_replay` proves they match; the existing mid-commit failure-injection/rollback tests stay green (no mixed state on partial failure). `sentinel-ctl restore` gains `--target-tick`/`--target-event-id`. Builds on the #491 PR-A prerequisites below.
- Issue #491 (TM-3 prerequisites, partially addresses #491): groundwork so an arbitrary tick/event can be restored exactly via anchor snapshot + bounded replay. The #490 spike traced the only replay divergences to state the snapshot dropped and to missing PSI input; this closes those gaps. **Snapshot v3** — `EcsSnapshot` now captures `autonomy_cooldowns` (per-agent, was respawned to default and re-fired autonomy right after the anchor) plus the four previously-dropped ephemeral resources `ActiveSmells`/`RoomChatBuffer`/`GaiaBuffer`/`BroadcastBuffer` as canonical JSON; `WorldSnapshot::SCHEMA_VERSION` 2→3 with a `v3→v2→v1` decode fallback chain (a `schema_version == 3` guard prevents an older byte-stream from being wrongly accepted as v3), so existing snapshots still restore. **PSI recording** — new sparse `DomainEventPayload::PsiBandChanged { cpu_above, mem_above }` emitted only on a band change (thresholds exported `pub` from `sentinel-bio`, re-exported via `sentinel-ecs`); since `apply_psi_stress` is purely threshold-based the band is a sufficient exact replay input. **Event hardening** — `ChaosTriggered.duration_ticks` and `AgentActionReceived.source` (`Option<String>`, both `#[serde(default)]`); `source == "autonomy"` lets replay skip autonomy-derived actions (the deterministic autonomy system re-creates them) instead of the old content-marker heuristic; existing persisted events still deserialize. **Determinism** — `task_progress_system` now runs `.after(autonomy_system)`, pinning the one Decision-set ordering ambiguity the spike's T6 probe flagged. **Replay queries** — `EventStore::get_events_range` / `count_events_in_range` / `max_event_id_at_tick` / `get_event_tick` provide the bounded `(anchor, target]` cursor lookups. **Anchor density** — `RetentionConfig.first_hour_interval_ticks` (default 300) makes `should_create_snapshot` take finer anchors during the first simulation hour and hourly afterwards (the first-hour *event* retention that keeps that window replayable is tracked by #250). **Shared hash** — the spike's canonical state-hash (sort component/cooldown vecs, drop transit UUID, canonicalize chaos/stimuli + the four new buffer JSONs, legacy-bincode f32, STRICT+CORE) is lifted into `sentinel_ecs::hash` and re-imported by `replay-spike` so there is one source of truth. The replay engine and `GET /operator/state-hash` build on this in the #491 PR-B.

### Fixed
- Console: **disabled buttons now read as inert.** `tokens.css` had no `button:disabled` rule, so `.primary` buttons kept their accent fill and pointer cursor and looked fully clickable even when disabled (surfaced during the #421/#422/#423 playwright verification — the wizard `Weiter`/`Deploy` and the editor save buttons). Added a global `button:disabled` rule (opacity 0.45 + `cursor: not-allowed`, no hover affordance; `.primary` keeps a dimmed accent so it stays recognizable). Affects the whole console.
- Issue #75 (follow-up, closes #75): **the bwrap `--info-fd` child-PID read is now robust under a spawn burst**, so the post-spawn netns isolation verifier no longer skips agents. Live on the deploy VM (full cage otherwise green: dedicated netns, loopback only, external blocked, no error flood) ~2/26 agents during a daemon-restart respawn burst logged `bwrap did not report a sandboxed child PID via --info-fd; verification will be skipped`. Root cause (confirmed with `strace` on bubblewrap 0.9.0, not a version/timeout issue): bwrap writes the info JSON across **eight `write()` syscalls** (one per field — the first chunk `{\n    "child-pid": N` is not valid JSON on its own), and the old code did a single `poll` + single `read` + `serde_json::from_str` on the whole buffer, so under load it caught the JSON mid-sequence → parse error → `None` → skip. `read_child_pid_from_info_fd` now **accumulates across reads, re-parsing until a complete JSON object is available**, re-polling against a total deadline, and logs the exact failure mode (poll timeout / poll error / EOF-before-complete / read error) so any residual skip is classifiable on the next deploy. A `File` now owns `read_fd` (RAII close on every path), which also removes one manual `libc::close` (unsafe baseline 26 → 25). New tests: `parse_child_pid_bytes_accumulates` (incomplete buffer → `None`, complete → `Some`), `read_child_pid_reassembles_chunked_writes` (replays the real eight-chunk write sequence over a `std::io::pipe` with gaps → the loop reassembles it), `read_child_pid_eof_before_complete_returns_none`. Kept `--info-fd` (one-shot, bwrap closes after writing — no late-write `EPIPE`) rather than switching to `--json-status-fd`; the read loop is the targeted fix. The acceptance gate (daemon-restart 26-agent burst → 0 skips + the forced-`share_net` fault-injection) is re-verified live at deploy.
- Issue #535 (closes #535): **`SnapshotManager::maintain()` no longer creates a double keeper for one calendar period within a single backlogged run** — the follow-up explicitly tracked in the #250 entry below. `maintain()` reads the world-snapshot list once and never reflects its own promotions, so two hourly siblings of the same calendar day/week/month both passed `has_snapshot_for_*` (which query the stale list) and both promoted. An in-run `promoted_periods: HashSet<(u8, i64)>` (tier discriminant + calendar key: day/week = `num_days_from_ce`, month = `year*100 + month`, reusing the #250 `calendar_*_key` buckets the ACs verify) now makes a sibling whose period was already promoted this pass redundant (→ `delete_redundant`) instead of promoting again. New unit test `maintain_backlog_promotes_one_keeper_per_calendar_period` (two hourly siblings on 2024-01-05 + one on 2024-01-06 → `promoted == 2`, not 3; same-day sibling deleted; the 06th promotes independently, so the #250 day/week/month dedup regression stays green). The `retention_bench` `kalender_dups` report is upgraded to a hard `assert == 0` across all 5 cycles plus a new `correctness_backlog` scenario (deploy-VM re-measure 2026-06-15: 4 siblings/day × 5 days → `promoted=5`, `kalender_dups=0`, was `20`/`15` under the old code; `maintain` p50 2.6/12.8 ms unchanged — the in-run set is O(1) per promotion).
- Issue #25 (closes #25): **eBPF Prometheus text exporter now escapes label values.** Agent names with inner quotes (real roster nicknames like `Tobias "Tobi" Lehmann`) were written raw into `:9090` label values, producing an unparseable exposition line (`sentinel_agent_stalled{...agent="Tobias "Tobi" Lehmann"}`) that a strict text-format parser aborts on. Added a central private `escape_label_value(&str) -> Cow<str>` in `crates/sentinel-ebpf/src/exporter.rs` (single-pass: `\` → `\\`, `"` → `\"`, line feed → `\n`; **carriage return stripped** — a strict Prometheus parser rejects a `\r` escape as an invalid escape sequence) and routed every dynamic string label value through it: `export_snapshot` (agent name, cgroup name, destination, PSI agent) and the legacy `export_io_profile` / `export_network` / `export_psi_stress` / `export_collector_meta` paths. Numeric (`cgroup_id`) and static (`direction`, names) values are untouched. The helper returns `Cow::Borrowed` (no allocation) for clean values — the common case — keeping the 1:n principle (no copy when unnecessary); a settings benchmark confirms the fast path is ~3.5× faster than always-allocating. New tests cover quotes/backslash/newline/CR-strip/borrowed-fast-path, a quoted-agent snapshot export, and a minimal label-value parser (local `promtool` replacement) asserting no raw `"`/LF/CR in any label value. New standalone benchmark `crates/sentinel-ebpf/benches/exporter_format.rs` (deploy-VM: ~48 µs/scrape at the realistic 26-agent population, escaping overhead ~16% for special-char names). No new metrics infrastructure: `:9090` stays a text-format export, no Prometheus/OTel server, library, or external stack is introduced (TOGAF Cluster 05b unchanged — escaping is invisible at the architecture level). Out of scope: kernel eBPF probes (userspace is the by-design productive default, behind the `ebpf` feature flag) and the `0.0.0.0:9090` wildcard bind (#525).
- Issue #250 (closes #250): **tiered world-snapshot retention made #264-immutability-conformant and its calendar dedup corrected.** The original AC-1 (`hourly == 24`) ignored the #264 7-day immutability window — under the ransomware/anti-tamper trigger `protect_recent_snapshots` (which `RAISE(ABORT)`s any `DELETE` of a snapshot younger than 7 days), the table is already bounded at `ceil(7d / hourly_interval) + first-hour anchors` (live: hourly 177 ≤ formula bound 180; series 211→179→172→177 is non-monotonic, i.e. converged — not a leak). AC-1 reformulated to that config-derived bound + promotion correctness; **Variant B chosen** (the #264 trigger stays absolute — the daemon is the untrusted component, so retention never young-deletes, only promotes within 7 days and deletes after; a retention-approved-delete exception was explicitly rejected as it would undermine ransomware recovery). `SnapshotManager::maintain` (`services/sentinel-daemon/src/snapshot.rs`) no longer swallows the trigger block via `.unwrap_or(false)`: redundant snapshots still inside the window are **skipped** (`kept_protected`) instead of attempted-and-failed, and any trigger block that does occur is counted+logged (`delete_blocked_young` = a drift alarm) rather than discarded; real DB errors propagate. The skip threshold and the trigger threshold are now the **single shared constant** `sentinel_limbo::IMMUTABLE_SNAPSHOT_MS` (fed into both the trigger `format!` and the daemon skip), with a tested boundary invariant (`const-1ms` → both skip/block, `const+1ms` → delete). **Promotion dedup is now calendar-aligned** (chrono, UTC): day = `date()`, week = Monday-anchored, month = `%Y-%m`, replacing the epoch-modulo buckets that broke weeks on Thu/Wed (1970-01-01 was a Thursday) and drifted months against 30-day blocks, producing duplicate keepers at period boundaries (live `daily` ran 9–13, target ~7). The Verify-SQL is documented in-code as the SSOT the Rust buckets must match. Adds `EventStore::save_world_snapshot_at` (explicit `created_at` for import/replay/tests/bench; `save_world_snapshot` delegates) and `count_world_snapshots_by_tier` (`GROUP BY tier`, for monitoring/AC-1). New tests: boundary invariant, trigger-error classification, week/month/day dedup reproduce-and-fix, `kept_protected`/`delete_blocked_young` paths against the real trigger, promotion preserves the restore anchor (payload/`last_event_id`/`tick` unchanged on tier UPDATE), and the #264 trigger-SQL ↔ constant SSOT linkage. New standalone benchmark `benches/retention_bench.rs` (deploy-VM, p50 ~2.3 ms at the realistic ~200-snapshot population, ~10 ms at 5× headroom; converges idempotently with zero drift alarms). Out of scope: the pre-existing stale-in-memory-list behavior where two sibling periods crossing a promotion threshold in one backlogged `maintain()` can both promote (a duplicate keeper) — unchanged by this issue (only the bucket function changed), rare under incremental hourly operation, tracked as a follow-up.
- Issue #530 (closes #530): bounded replay now reproduces the **anchor's** `delta_seconds` for its per-tick time step instead of a hardcoded `1.0`, so `restore(anchor) + replay` is byte-exact at any constant `time_scale`, not only `1.0`. `replay_loop` (`services/sentinel-daemon/src/replay.rs`) read the anchor delta from the restored `SimulationTime` (set by `restore_ecs_state` from `EcsSnapshot.sim_delta_seconds`); the live loop uses `delta = tick_rate × time_scale` (`orchestrator.rs:4117`), and both `sim_hour`/`sim_delta_seconds` (hashed in STRICT+CORE) and every delta-dependent system (bio integration) would otherwise diverge under acceleration — a silent wrong-state restore, the worst failure mode for the Time-Machine guarantee. At `time_scale == 1.0` the anchor delta is `1.0`, so behavior is unchanged (no #491 regression; existing `live_equals_restore_plus_replay` stays green). New test `replay_reproduces_anchor_delta_at_nonunit_time_scale` (delta = 2.0) proves STRICT+CORE equality. Out of scope: per-tick varying delta from adaptive tick-rate changes mid-window (a single anchor delta cannot reproduce that; would need a sparse recorded-delta input like `PsiBandChanged`) — exact for any constant `time_scale`. Unblocks the #529 ad-hoc forced-shift (`time_scale = 600`) verification.
- Issue #491 (TM-3 follow-up): fixed anchor selection for tick/event restore so it never picks a snapshot whose tick is *after* the target. During quiet periods several snapshots share the same `last_event_id`, so the old `last_event_id <= target_event_id` check alone could choose a too-new anchor → an empty replay range `(anchor_tick, target_tick]` → the world stayed at the anchor state instead of the target (caught by live VM verification, where the snapshot at tick 2540056 was wrongly chosen for target tick 2540040 and `ticks_replayed=0`). The anchor now additionally requires `tick < target_tick`. Extracted the choice into `select_anchor_snapshot` with a regression test covering the shared-`last_event_id` case. The in-process integration test never hit this because its anchor was always controlled to be before the target.
- Closed Issue #520: `cargo update -p stabby -p stabby-abi` bumps the yanked `stabby`/`stabby-abi`/`stabby-macros` 72.1.1 (transitive via `zenoh-plugin-trait` 1.7.2) to 72.1.4, turning the `Advisories` cargo-deny job green again on main and PRs; the resolution also drops the now-unused duplicate `syn 1.0.109` and `proc-macro-crate 3.4.0` from the lockfile. No zenoh version change; workspace tests/clippy and the sentinel-zenoh SHM benchmark guard against regressions.

### Changed
- Initial Issue #75 implementation: **agent network model formalized as full cage; dead bridge/veth/nftables path removed; post-spawn isolation verifier added.** Agents make no network calls (`services/agent-runtime` has no network code; `services/sentinel-daemon/src/config.rs:438` — LLM traffic is proxied by the daemon→gateway on the host, `claude -p` runs inside the gateway), so the half-built `crates/sentinel-sandbox/src/netns.rs` bridge/veth/nftables connectivity is removed entirely. That path only ever logged `Netns setup fehlgeschlagen` on the VM because it was handed the bwrap **supervisor** PID — which stays in the root netns by design — instead of the sandboxed child PID, so the veth never reached the agent. `BwrapConfig::for_agent` now sets `share_net=false` unconditionally (`bwrap --unshare-all` without `--share-net` → own netns, loopback only); bwrap is spawned with `--info-fd` (`libc::pipe2` + `pre_exec`) so the daemon obtains the sandboxed **child** PID (`SpawnedSandbox.child_pid`, bounded `poll` + accumulating read). After spawn the daemon calls `SandboxEnforcer::verify_agent_netns_isolation(child_pid)` comparing `/proc/<child_pid>/ns/net` to `/proc/self/ns/net` → `IsolationStatus::{Isolated, NotIsolated, ProbeError}`: `Isolated` continues; `NotIsolated` (agent shares the daemon netns, e.g. a forced `share_net`) emits a structured warning + a **string-typed** `AgentIsolationFailed` event (built via `DomainEvent::new` — no new `DomainEventPayload` variant, no `sentinel-common` change) + a security health snapshot and terminates the un-caged process (ECS-only fallback) — never a silent un-cage; `ProbeError` (transient `/proc` read) warns only and never terminates (the bwrap exit code is the primary fail-closed signal). Removed `netns.rs`, `SandboxWarning::NetnsNotAvailable`, `detect_netns_support`, `SandboxEnforcer::{setup_network, has_netns}`, `SandboxHandle.netns_config`, and the veth/nftables/bridge benchmarks; added `bench_netns_verify` (verifier overhead). Acceptance tests rewritten for full cage (`ac_75_01_agent_default_full_cage` + VM-only distinct-netns and loopback-only checks). No external network stack introduced and the daemon→gateway MITM path is unchanged (TOGAF agent-network lines `--share-net` → `--unshare-all` updated separately by the main session).
- TOGAF architecture guide (clusters 06/07): deploy-VM memory right-sized from 24 GB to 8 GB (#517) after a host-level OOM kill; hardware table, memory-budget card (headroom now ~2-4 GB), and the VM host-level tuning row updated. Measured normal load is <1 GB. Boot defaults on the VM are now token-safe via systemd `ConditionPathExists=/etc/sentinel/allow-llm` drop-ins for gateway/judge/health-monitor-timer (VM-side deliberate drift; repo units unchanged).
- TOGAF architecture guide (clusters 05/07) now reflects the SOTA console stack delivered by Epic #430: the language-selection and stack tables describe SolidJS + Rust (axum/wtransport, :8001) with WebTransport/QUIC event push and msgpack+zstd delta frames instead of the removed Bun/Hono + WebSocket-poll dashboard; the service table and the `sentinel.target` startup chain reference `sentinel-dashboard-backend`.

### Removed
- Removed `docs/benchmarks/BENCHMARK-RESULTS.md` from the repository and gitignored `docs/benchmarks/`: benchmark result registers are internal working artifacts (same policy as the other entries under "Internal working artifacts" in `.gitignore`); the measurement data lives in the internal benchmark register outside the repo.

### Security
- Closed Issue #542: bumped **`wasmtime` + `wasmtime-wasi` from `44.0.2` to `45.0.2`** to clear **RUSTSEC-2026-0182** ("Leak in WASIp1 `fd_renumber`", [GHSA-3p27-qvp9-27qf](https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-3p27-qvp9-27qf)). The advisory was published after `main`'s last green `Security Audit` and was failing the `Advisories` / `rust-audit` / `Security audit` jobs repo-wide on every PR; `wasmtime` is a real default dependency (via `crates/sentinel-wasm`, enabled by `sentinel-daemon`'s default `wasm` feature, and used by the `sentinel-ecs` `ToolRuntime` + the `WasmtimeNanoRuntime` adapter). The `44 → 45` major bump was chosen over the minimal `44.0.3` patch to stay current; it carried **no embedder API delta** for our usage (`component::bindgen!` / `WasiView` / `WasiCtxView` / `wasmtime_wasi::p2::add_to_linker_sync` / `component::Linker` / `HasSelf` / `Engine` / `Store` / `Config` / `StoreLimits` all unchanged), so it is a dependency-only change — no `sentinel-wasm` source edit. The lockfile delta is the `wasmtime`/`cranelift`/`pulley` + WASI dependency stack only (resolved with `--precise`, no opportunistic transitive bumps; the WASI RNG impl moved from `cap-rand` to `chacha20`/`rand`). Verified: `cargo deny check advisories` → `advisories ok`, `cargo audit` → 0 vulnerabilities, `sentinel-wasm --features wasm` 114 tests green (real WASM execution incl. capability/memory-exhaustion/no-state-leak), `cargo check --workspace` + `clippy --workspace -D warnings` green. Same pattern as #376 (Wasmtime 44) / #520 (stabby). Not touched: the `wasm` default feature itself (WASM tool-sandbox stays) and the already-triaged `deny.toml` ignores (rsa RUSTSEC-2023-0071 via zenoh, bincode RUSTSEC-2025-0141).
- Hardened Issue #474 dashboard operator auth (Attacker Class 2 production gate): added a per-client-IP **login rate limit + audit logging** to `sentinel-dashboard-backend`. `POST /api/auth/login` now blocks an IP after `SENTINEL_DASHBOARD_LOGIN_MAX_FAILS` (default 5) failures within `_WINDOW_SECS` (default 60) for `_BLOCK_SECS` (default 300), returning `429` + `Retry-After` **before** the constant-time key comparison (no key oracle during a block; a correct key also returns `429` while blocked), and a successful login resets the counter. Failed attempts are audit-logged via `tracing::warn!` (never the attempted key). Own ~80-LOC fixed-window limiter (no new dependency / `governor`); only `login()` is limited (`wt`/`require_auth`/`cert-hash` untouched); client IP via `ConnectInfo` (`0.0.0.0` fallback keeps `oneshot` tests from 500-ing). The systemd unit binds **loopback-only** (`127.0.0.1:8001`, HTTP + WT) as the secure default — deliberate exposure overrides `SENTINEL_DASHBOARD_*_BIND` via the env-file (TLS/exposure model: #473). The SolidJS console shows a distinct "too many attempts" message on `429`. New runbook `docs/security/dashboard-key-management.md` (key generation/storage/rotation, rate-limit knobs, exposure decision); `docs/security/threat-model.md` updated. Remote live console over an `ssh -L` tunnel is HTTP-only (WT/QUIC = UDP); full remote access is tracked in #522. 7 unit + 5 integration tests cover the limiter semantics.
- Closed Issue #477: bumped the Go toolchain directive from `go1.26.3`/`.2`/`.1` to **`go1.26.4`** across `go.work` and all Go-module `go.mod` files (`cmd/cortex-gateway`, `pkg/sentinel-go`, `services/sentinel-judge`, `services/sentinel-nats-bridge`). This clears two standard-library vulnerabilities that `govulncheck` started flagging on 2026-06-03: **GO-2026-5039** (`net/textproto`, arbitrary inputs in errors without escaping — reachable via `cortex-gateway` `ClaudeProvider.StreamHTTP` → `ReadMIMEHeader`) and **GO-2026-5037** (`crypto/x509`, inefficient candidate hostname parsing). Both are fixed in `go1.26.4`. No functional or dependency changes (x/crypto is already on 0.45 via #386).

### Changed
- Reworked Issues #487/#488/#489 Time-Machine restore into a fenced world-state transfer path: restore now takes a fatal pre-restore rollback snapshot, validates referenced sentinel-fs CAS blobs before commit, fences runtime reconcile/config/snapshot/prune-style writers, commits redb/fs/ECS/projection/event state through one helper, rolls back mid-commit failures under the same fence, and leaves the fence active with a critical error if rollback itself fails. Projection restore now resets and seeds `agent_live_view`, occupancy-only `room_live_view`, `kpi_1m`, `task_kanban`, and `projection_watermarks` in one SQLite transaction; `RoomPhysicsState` remains explicitly out of scope for reconstruction. Restore postcommit tears down stale runtime fragments and respawns the restored world through the shared runtime spawn stack without duplicating ECS entities.
- Added Issue #279 periodic in-process runtime reconcile inside `sentinel-daemon`: the operator-triggered reconcile and the automatic tick-loop reconcile share the same execution path, runtime health now exposes reconcile source counters, and the default control-plane config keeps `sentinel-gateway` out of `monitored_services` so token-generating services are not auto-restarted.
- Hardened Issue #279 runtime cleanup: agent process handles are terminated/reaped before sandbox teardown, occupied cgroups go through the kill/wait path before removal, and gateway-down LLM stalls set `llm_circuit_open` so platform rules suppress only stall `RestartAgent` churn while leaving service-health restarts intact.
- Updated Issue #279 VM evidence scripts: soak assertions derive the active-agent target from `expected_active_agents`, dashboard API checks count only `status="active"` rows, reconcile source fields are captured, cgroup-remove warnings fail the soak, and runtime-reconcile benchmark names now use the measured active-agent count instead of a hard-coded 26.
- Closed Issue #378: degraded agents (bwrap-start failure, missing cgroup, or sandbox-setup error) are now recorded in the daemon `security_runtime_state` with `bwrap_pid = None` and the `/ram/agents/<name>` home fallback, so the Operator Security API no longer loses sight of agents running without full isolation. Added regression tests `degraded_agent_recorded_in_security_runtime_state` (degraded `None` path) and `healthy_agent_records_pid_and_mount_home` (PID + mount contrast) in `services/sentinel-daemon`.
- Closed Issue #463 auth inconsistency in `sentinel-dashboard-backend`: the four projection read routes (`/api/agents`, `/api/rooms`, `/api/metrics`, `/api/tasks`) are now behind `require_auth` (grouped sub-router + `route_layer`), consistent with the control plane — the real sim projection data (agent names/roles/bio-state, rooms, KPIs, tasks) is no longer readable unauthenticated over HTTPS GET (a #431-legacy gap found in the Phase-2 live smoke). `/api/cert-hash` deliberately stays public (the browser needs the cert hash *before* login for WebTransport `serverCertificateHashes` pinning, documented in the route comment); `/api/auth/*` and `/api/wt-ticket` are unchanged. The console is unaffected (it gets agents via the WebTransport push, not these GET routes). The router construction moved into `build_app()` so the auth gates are integration-testable (`tower::ServiceExt::oneshot`: unauth → 401, valid session → reaches the handler, cert-hash → 200 unauth).
- Replaced the historical fixed 60-agent `AgentId` ceiling for Issue #414 with explicit configurable bounds used by Agent TOML loading, daemon dry-run/orchestration, Judge-Alert AgentId parsing, and nightrun loading. The default remains 60 for existing configs, while Gaia-generated companies can validate larger cohorts end to end.
- Superseded Issue #396's DEV-006 WASM-default runtime decision with Issue #407 DEV-007: the Nano-Container platform now uses a runtime-agnostic CRI-style contract with explicit per-workload runtime selection and no global default runtime.
- Hardened Issue #402 dashboard operator auth into a real XSS defense: the operator key is now exchanged once at login (`POST /api/auth/login`, constant-time compare) for a random server-side session token (in-memory `Map<token, expiry>`, 12h TTL, revocable via `POST /api/auth/logout`), delivered as an `httpOnly` + `SameSite=Strict` cookie. The key is never held in JS again; `requireAuth` validates the cookie instead of an `Authorization` header; `api-key.js` was removed and `control/floorplan/timetravel.js` send the cookie via `credentials:'same-origin'`. `DASHBOARD_COOKIE_SECURE=auto|on|off` controls the `Secure` flag (auto detects HTTPS via `X-Forwarded-Proto`). **Residual (documented in `docs/security/threat-model.md`):** key+cookie transit cleartext over plain HTTP on the LAN deploy VM — no regression vs the old Bearer scheme; production belongs behind an HTTPS proxy with `Secure` on. The read-only telemetry WebSocket stays intentionally unauthenticated (no state changes).
- Moved Issue #400 dashboard operator API key out of `sessionStorage` into a shared in-memory module (`dashboard/public/js/api-key.js`), consumed by `control.js`, `floorplan.js`, and `timetravel.js`. This clears the CodeQL `js/clear-text-storage-of-sensitive-data` high alerts and removes the threefold duplication of the `getApiKey`/`authHeaders` helpers. **Honest scope:** this fixes the scanner alert and the duplication — it is **not** real XSS hardening (an in-memory variable is just as readable to same-context XSS as `sessionStorage`); genuine token-theft hardening (keeping the key out of JS-accessible storage via httpOnly cookie / server-side auth) is tracked as a separate follow-up issue. Trade-off: the key no longer survives a full page reload (re-enter once per session).
- Activated Issue #380 eBPF production evidence end to end: daemon/dashboard deploy verified `mode=kernel`, no-CAP `userspace` fallback, TCP request deltas, dashboard eBPF cards without `N/A`, and Deploy-VM overhead of 0.012658% amortized on the i7-3930K VM.
- Finalized Issue #382 NMDA episode-selection policy for hippocampus/nightrun: calibrated threshold `0.25`, shared narrative threshold, max 10 selected episodes, selection-quality payload metrics, and Deploy-VM replay evidence for a `2/3` consolidated Night-Run.
- Moved Issue #278 nightrun/shift evolution LLM calls out of the ECS tick loop into an async background task, with VM evidence for fake-gateway success, gateway-down fail-safe behavior, and redb `EVOLUTION_VERSION` writes.
- Added and optimized Issue #379 live sentinel-fs FUSE/CAS verification paths for storage stats and dedup-hit benchmarking; Deploy-VM evidence shows active FUSE agent homes, 99.22% dedup savings, and same-VM median dedup-hit write p95 improved from 40,411 us to 189 us, while the strict `<100us` target remains unmet on the i7-3930K VM.
- Updated optional `sentinel-wasm` Wasmtime dependencies to 44.0.2 to clear current RustSec advisories.
- Optimized Issue #277 dashboard WebSocket change detection from five per-view polling queries to one Projection-DB `projection_watermarks` lookup per poll cycle, with idle polling skipped when no WebSocket clients are connected; VM benchmark evidence is captured with `dashboard/scripts/measure-ws-polling.sh`.
- Optimized Issue #276 ECS tick-loop hot paths: reusable room-physics and persist workspaces, perception buffer reuse, and batched Limbo event/outbox writes. Deploy-VM benchmarks on Intel i7-3930K show relative improvements of 26.86% physics, 26.34% perception, 52.57% persist e2e, 86.95% persist write-only, and 17.23% full tick.
- Excluded `sentinel-gateway` from daemon platform-controlplane `monitored_services` in the deployment config so benchmark and smoke runs can keep the gateway stopped without self-heal restarts.

### Added
- Added Issue #490 replay-spike determinism harness (experimental, `services/sentinel-daemon/src/bin/replay-spike.rs`) — the Go/No-Go gate for Arbitrary Restore (TM-3). It drives genuine re-execution of the ECS tick loop with tick-pinned scripted inputs and compares canonicalized ECS state hashes (STRICT + CORE; N1 sort component vecs, N2 drop the per-action `transit_correlation_id`, N3 canonical chaos/stimuli JSON, N4 f32 bit-pattern via legacy bincode). Test ladder T1–T7 (engine determinism, restore-vs-live, two replays, event-log-as-input, per-tick trace, order-probe, PSI negative control) run in-process and cross-process. **Verdict: GO for TM-3, conditional** — the engine is deterministic same-machine (T1/T3/T6/T7 pass at all windows incl. 10 000; cross-process identical) and bounded replay is byte-identical to the live run when the snapshot captures complete state (zero-PSI T2 perfect through 10 000 ticks). The only failures are the enumerated, predicted restore-coverage gaps (`AutonomyCooldown`/`ActiveSmells`/`RoomChatBuffer`/`GaiaBuffer`/`BroadcastBuffer` missing from `EcsSnapshot`), traced by T5 to the exact post-anchor divergence. The event log is a faithful input log for agent actions (T4: 78/78 reconstructed). Full analysis + TM-3 prerequisites in `docs/spikes/SPIKE-490-exact-replay.md`; benchmarks (break-even live-compute vs replay) and raw evidence on the deploy VM. Kept as the TM-3 reference; not production.
- Added Issue #381 per-phase timing with an internal profiling path (Cluster 05b, **no export**): opt-in `install_phase_timing` in `sentinel-ecs` adds 11 boundary marker systems around the ten chained `SimulationPhase` sets and stamps wall-clock marks into a new telemetry-only `PhaseTimings` resource (no `Commands`/deferred params → no new sync points; a determinism-guard test proves two ticks with vs. without markers produce identical bio state, positions and event counts). The daemon (config `phase_timing_enabled`, default `true`) records the per-phase durations after `schedule.run` into per-phase histograms in the existing `MetricsRegistry` (`sentinel.ecs.phase.{phase}.duration_ms`, 16 log-spread bucket boundaries fixed by the #381 VM sweep) and the existing :9090 Prometheus text render now emits them as one summary family `sentinel_phase_duration_ms{phase="input"…"persist"}` (p50/p95/p99 + `_sum`/`_count`; `HistogramSnapshot` gained a `p95` field). The dashboard-backend exposes them via a new auth-gated read-proxy `GET /api/metrics/phases` (canonical phase order, parser unit-tested), and the console gains a new `profiling` panel (`ProfilingView`) with p50/p95 bars per phase, 5s polling and a degraded fallback. **No OTLP/OpenTelemetry export** — the original export plan stays rejected (1:n block/byte principle); the information lives once in the registry and is read through the existing :9090 → dashboard-backend → console path. VM benchmarks (i7-3930K, criterion + vmstat/mpstat sidecars in `console/evidence/issue-381-live/`): histogram observe 30.0 ns (16 buckets), full 10-phase record path 280 ns/tick, full-tick delta with vs. without instrumentation within measurement noise (129.78 vs 129.54 µs, 26 agents) — far below the 0.1% tick-budget cap (AC-4).
- Added Issue #505 cortex-gateway `local-loop` provider for token-free agent E2E tests: the gateway can be switched via `CORTEX_LOCAL_LOOP=1` or `CORTEX_PRIMARY_PROVIDER=local-loop` to answer both `/internal/llm` and Anthropic-compatible `/v1/messages` locally with deterministic action-format responses. The provider supports non-streaming JSON and Anthropic SSE (`message_start` through `message_stop`), optional JSONL scenarios, synthetic token usage, traffic-stat visibility, and zero upstream/cost booking; default remains off.
- Added Issue #420 dashboard config Read+Write in the Rust backend (`sentinel-dashboard-backend`, Epic #418 Configure/Build, on the #431 SOTA stack): a new auth-gated `/api/config` route group (distinct from `/control/config`, which proxies the Gateway LLM config). **READ** serves the platform config parsed as JSON to the SolidJS editors — `GET /api/config/agents` (all `config_dir/agents/AGENT-*.toml` via `load_all_agents_with_validation`), `GET /api/config/rooms` (`BuildingConfig::load`), `GET /api/config/daemon` (daemon.toml raw text, read-only). **WRITE** `POST /api/config/apply` validates the inline `OperatorConfigApplyCommand` (`{mode, agents, building}`) and on success proxies it to the daemon Operator-API `/operator/config/apply` (#425). **Architecture (Option B):** the dashboard is a **validator + apply-proxy** — it never writes `config_dir` itself; the daemon stays the sole config writer (durable atomic write + pre-apply backup, daemon-internal #425). This keeps the network-exposed dashboard read-only (security, #474) and avoids duplicating the persistence logic. The pre-validation is an **exact subset** of the daemon's `validate_config_apply` (never stricter — invalid → `400 {errors}` with no upstream call; valid → forward): agent-count ≤ `max_agents`, `BuildingConfig::validate` (bidirectional adjacency + capacity), no duplicate agent IDs, per-agent personality bounds `[0,1]`, AgentId bounds. `max_agents` is read from the same `config_dir/daemon.toml` `[daemon].max_agents` (single source, no drift; lenient fallback defers the bound check to the daemon). `config_dir` is env-configurable (`SENTINEL_CONFIG_DIR`). Integration tests cover the auth gate (401 without session), parsed READ, invalid-JSON → 400, invalid-adjacency → 400 with no upstream call, and valid → forward. Frontend views (#418 subs), the daemon apply logic (#425), the transport (#431), and daemon.toml writes are out of scope; productive `NanoRuntimeRegistry` wiring is unrelated (#472).
- Added Issue #417 microVM Nano-Container runtime adapter (`crates/sentinel-microvm`, Cluster 12 / DEV-007): a fourth `NanoRuntime` family alongside ECS-native, WASM/Wasmtime, and bwrap+Landlock, able to run a workload as a hardware-virtualized **Firecracker** microVM over KVM. **Adapter + conformance harness only** — like all four NanoRuntime adapters it is reachable through the `NanoRuntimeRegistry` in tests + `bin/nano_runtime_bench.rs` but is **not yet wired into the productive daemon spawn path** (`spawn_agent_full` uses `SandboxEnforcer`/`RuntimeOrchestrator` directly); that productive registry integration is tracked in #472 (open). Implements the full seven-operation contract (`spawn`/`exec`/`snapshot`/`restore`/`health`/`isolate` + the default-composition `migrate`) via a dependency-free HTTP-over-Unix-socket client for the Firecracker API (machine-config, boot-source, rootfs drive, vsock, InstanceStart, pause/resume, snapshot create/load, InstanceInfo). New snapshot semantics `MicrovmMemory`: the `NanoSnapshot.payload` carries **stable metadata** (config + deterministic Firecracker mem/state paths per `workload_id`), not the volatile guest RAM — keeping `restore(snapshot(x))` payload-stable for the #408 conformance harness while the real memory snapshot lives in the referenced Firecracker files. `spawn` fails with a clean, KVM-naming error when `/dev/kvm` is absent (AC-4). Minimal-setup (kernel/rootfs/work-dir) is env-configurable (`SENTINEL_MICROVM_*`). KVM-dependent conformance is `#[ignore]` (Deploy-VM, `cargo test -p sentinel-microvm -- --ignored`); the KVM-free clean-error path runs everywhere. Greenfield PoC (prio:low / infrastructure stock); cross-node migration stays out of scope (Multi-Node-gated).
- Added Issue #413 local live-migration as an observable first-class operation for the ECS-native Nano-Container runtime (Cluster 12): a new operator endpoint `POST /operator/migrate` (auth-gated) triggers a tick-synchronous, orchestrator-driven snapshot→restore→terminate handoff of the live world's ECS instance via the `NanoRuntime::migrate` contract — the live daemon world stays untouched (the migrated instance is derived from a read-only world snapshot). Emits a new `MigrationCompleted` domain event (runtime_key, workload_id, agent_count, from/to handle, `duration_ms`, reason) into the Time-Machine event store and increments a `sentinel_daemon_migrations_total` telemetry counter. Roundtrip-invariant and unit tests cover identity preservation through the handoff. **Honest scope:** LOCAL same-host migration only; cross-node/network transfer, per-agent and cross-runtime migration, and a zero-downtime guarantee are out of scope (Multi-Node-gated; the Raft/consensus layer is documented as still missing).
- Added Issue #464 Event-Log CAS block-delta path inside `sentinel-dashboard-backend`: the #433 append-only `event_log` payload stream is replaced by an Event-Log CAS plane backed by `sentinel-console-plane`; the poller ingests read-only `events.db` rows into content-addressed zstd blocks and broadcasts only lightweight `event_log_cas_tick` sync triggers. The same WebTransport `:8001` connection now accepts byte-compatible length-prefixed JSON bi-stream requests (`HelloManifest -> EventLogCasResponse`) with an ordered block manifest, missing-block `Delta`, and transfer/dedup stats; `agent_live`/`room_live`/`kpi` uni-stream reconcile paths stay unchanged. The SolidJS transport client sends its cached block manifest, reassembles the ordered Event-Log into the existing append-only store slice, and keeps the Activity/Chaos/Chat consumers unchanged. The standalone #439 `sentinel-console` service and systemd unit were removed because the CAS transport is now integrated into the dashboard backend and has no remaining consumer. Tests cover partial-manifest transfer below 20% of full raw state, WT bi-stream roundtrip reassembly, and frontend CAS cache/reassembly.
- Added Issue #433 PR-D final proxy views for the strict 9-view SolidJS migration: the console now renders `control` and `timetravel` panels from the Rust backend surfaces. Control covers pause/resume, provider and agent-provider override controls, LLM parameters, pipeline hardening, guardrail/traffic/platform state, platform analyses, and live config degradation. Timetravel covers snapshot timeline/list/detail and restore/create actions through the auth-gated Operator proxy. `sentinel-dashboard-backend` now also exposes public `/api/health` for deploy smoke/monitoring. Live VM evidence captures old/new screenshot pairs for Control and Timetravel, with the console transport connected, Gateway/Judge kept inactive for the 0-token gate, Control degrading to `Gateway offline`, and Timetravel showing 166 snapshots plus world-state detail. After verification, the old Bun/Hono `dashboard/` tree, stale dashboard E2E scripts, and the `sentinel-dashboard.service` deploy path were removed; CI, CodeQL, dependency/security checks, demo Docker, deploy smoke, and health-monitor now target `console/` plus `sentinel-dashboard-backend` on HTTPS/WebTransport `:8001`. This is the final #433 PR and leaves #464 CAS block-delta as the separate follow-up.
- Added Issue #433 PR-C event views for the strict 9-view SolidJS migration: `sentinel-dashboard-backend` now publishes an append-only `event_log` WebTransport topic from read-only `events.db`, with connect backfill (recent window plus focused chaos/chat action events), idle skip, 500-event append batches, and the same topic+msgpack+zstd codec path as the other live topics. The SolidJS console now renders `activity`, `chaos`, and `chat` panels from one capped append-only event slice, with dedupe by event id, filters/search/room filters, VirtualScroller-backed lists, and operator chat send via the existing auth-gated proxy. Live VM evidence captures old/new screenshot pairs and proves the append count increasing from 9946 to 10000 events. After verification, the old Bun view paths and API routes for Activity, Chaos, and Chat were removed and deployed; those old paths now return 404 on the VM. #464 CAS block-delta remains separate.
- Added Issue #433 PR-B SolidJS push views for the strict 9-view console migration: the console now consumes `agent_live`, `room_live`, and `kpi` topic frames for real `agents`, `floorplan`, `metrics`, and `cockpit` panels. Agents render status, transit target, eBPF stall state, last action, mood, and six bio bars. Floorplan renders room cards from `room_live` plus static room metadata and opens `/api/rooms/{id}/detail` with physics/chaos/stimulus/reaction history and authenticated chaos/stimulus trigger forms. Metrics renders KPI cards plus `/api/metrics/{ebpf,pipeline,tick}` graceful extras and static #276 benchmark evidence. Cockpit renders `/api/cockpit` as a SLO status bar plus severity-sorted incident list. Live VM evidence captures old/new screenshots for all four views. After verification, the old Bun view paths for these four views were removed from `dashboard/public` navigation/imports/renderers; shared Bun API routes remain for not-yet-migrated views.
- Added Issue #433 PR-A backend foundation for the strict 9-view SolidJS migration: `sentinel-dashboard-backend` now exposes the missing auth-gated console data surfaces and proxy surfaces before frontend migration starts. Projection helpers `rooms_rows()` and `metrics_row()` are reusable from both HTTP routes and push code; `event_sub` now classifies events into `agents`/`rooms`/`kpi` dirty flags and pushes additional `room_live` and `kpi` WebTransport topic frames through the existing `topic+msgpack+zstd` codec while preserving the 150ms coalescing tick and idle skip. New read surfaces cover `/api/cockpit`, `/api/rooms/{id}/detail`, `/api/metrics/{ebpf,pipeline,tick}`, `/api/events`, and `/api/events/types`; Gateway/Operator proxy surfaces cover pause/resume, status, agent-provider override, traffic/platform reads, snapshots, restore, and operator chat. `SENTINEL_EVENTS_DB`, `CORTEX_GATEWAY_PROXY_URL`, and `SENTINEL_DAEMON_PROMETHEUS_URL` configure the new read/proxy paths. Cockpit/events/metrics-extra routes degrade to authenticated 200 responses with offline/empty status when `events.db`, Gateway, Judge, or Prometheus are unavailable, matching the 0-token smoke requirement. Tests now cover route auth, graceful degradation, projection helpers, and `room_live`/`kpi` push frames.
- Added Issue #432 event-stream push for the dashboard backend (Epic #436 Phase 2): `sentinel-dashboard-backend` now subscribes to the engine event bus (NATS JetStream `SENTINEL_EVENTS`) and pushes an `agent_live` frame to the WebTransport clients on every agent-relevant engine change — **replacing the old 1s projection poll** (no poll, idle ~0 work). New `event_sub` module opens an **ephemeral** pull consumer with **`DeliverPolicy::New`** (only events from connect onward — no 7-day backlog replay; the connect-snapshot covers the current state) and `InactiveThreshold=30s` (no server-side state leak on disconnect; `dashboard-live` is only a log label, not a durable name). Each event is classified (`classify`): agent-relevant events (spawn/despawn/action/transit/status/bio/shift) and `config_applied` (resync) trigger a push; pure room/KPI/task events do not; **any unknown/new type fail-safe triggers a push** (a new event type must not silently stale the view). Pushes are coalesced over a 150ms tick: the agent read-model is re-read **once** via `projection::agents_rows()` and broadcast as one `agent_live` topic frame; the client's `reconcile({key:"agent_id"})` turns the full set into the DOM delta (despawn + resync free) — the chosen strategy over CAS block-delta for the small agent set (CAS for large append-only streams = a tracked follow-up issue). **Idle parity** with the old poll (#277): when no WebTransport clients are connected, the tick skips the DB read entirely. Each WebTransport session subscribes to a `tokio::sync::broadcast` channel after its connect-snapshot and writes each frame as its own uni-stream (`Lagged` → skip, next snapshot is authoritative). The subscriber is a daemon task with reconnect backoff (500ms→30s) and never panics/exits when NATS is down (the connect-snapshot + HTTP keep working). **Scope:** only `agent_live` is pushed — `room_live`/`kpi`/`tasks` have no console consumer yet and belong to the 9-view migration (#433); no frontend change.
- Added Issue #444 tiling layout engine for the console (Epic #436 Phase 2, niri/Hyprland-style): a lightweight signal-driven binary-tree tiling engine (`console/src/tiling/engine.ts`) — leaf=panel, split=two children (row|col) with a `fraction`; ops `splitLeaf`/`closeLeaf` (parent collapses onto sibling)/`resizeSplit` (clamped 0.1–0.9)/`openPanel` are pure tree transforms in a SolidJS store (fine-grained reactive, no full rerender). `TilingLayout.tsx` renders recursively on CSS Grid (`grid-template-{columns|rows}` from the fraction) with a draggable gutter (pointer-drag → fraction), `ResizeObserver`-cached container rect, smooth re-tiling via CSS transition + Web-Animations-API fade-in of new panels (GPU transform/opacity). Three-pillar default workspace (Dashboard·Control-Center·Chat); Gaia can open panels contextually (`openPanel('floorplan')`); mobile breakpoint switches to BottomTabBar. Verified: vitest 5/5 (tree ops) + **playwright-cli optical verification** (initial workspace, after Gaia-open-floorplan, after split, gutter-resize before/after, after close, mobile tab-bar — screenshots reviewed + confirmed) + frame-timing during re-tiling (max 16.8 ms gap, 0 frames >50 ms = steady 60fps). Functional design only (visual polish is a separate pre-go-live phase).
- Added Issue #419 SolidJS app-shell on the Rust/WebTransport backend (Epic #436 Phase 2, DEV-009): new `console/` SolidJS project (vite + vite-plugin-solid) — the reactive base for all console views, served by the #431 backend. **Transport client** (port of noaide `transport/client.ts`+`codec.ts`): WebTransport connect with `serverCertificateHashes` from `/api/cert-hash`, server→client unidirectional-stream frame parser, `fzstd` zstd-decode + `@msgpack/msgpack`, UUID-16-bytes→string, exponential-backoff reconnect. **State** = `createStore` + `reconcile({key})` delta-merge + version signals; pushed `agent_live` frames reconcile reactively into the DOM. **Shell** = three pillars (Dashboard · Control-Center · Chat) on desktop, `BottomTabBar` on mobile (matchMedia breakpoint), CSS-variable theme (`tokens.css`), httpOnly-session login flow (`credentials:'include'`). **Reusable controls** (ProgressBar/SearchFilter/StatusDropdown/LiveIndicator/ThemeToggle/Toast) + **VirtualScroller** (fixed-height + overscan, smooth at N=10000). Docs `console/docs/new-view.md`. Verified: vitest 5/5 (codec helpers + store reconcile delta-merge), `vite build` + `tsc` green, and **playwright-cli optical verification** of every visible element (login, 3-column shell, controls, reactive view before/after a pushed frame, VirtualScroller top + scrolled, mobile BottomTabBar, dark theme) — screenshots reviewed + confirmed. Live WebTransport transport + reconnect verified in the combined Phase-2 VM smoke against the deployed #431 backend.
- Fixed/Added WebTransport browser-auth + connect-snapshot for the dashboard backend (Epic #436, follow-up to #431, kicks off #432): the live VM smoke uncovered that browser WebTransport could never connect — (a) the WT endpoint was on a different port (:4434) than the page (:8001) → cross-origin; (b) WebTransport handshakes carry NO cookies, so the httpOnly-session-cookie auth rejected every browser. Fixes: WT now binds the same port as HTTPS (:8001 UDP, same-origin); auth via a short-lived single-use **ticket** (`GET /api/wt-ticket`, require_auth; consumed at the handshake from `?t=`) instead of a cookie; the server pushes an **agent_live connect-snapshot** (read-only `projection.db`, reusing `projection::agents_rows`) so the console renders the real agents reactively on connect, and keeps the session open (`connection.closed().await`) so the LiveIndicator shows `connected` (continuous delta push = #432). Verified live on the VM (loopback browser: `connected` + 43 real agents in the DOM); 8 unit + 2 WT integration tests + clippy `-D warnings` green.
- Added Issue #431 SOTA dashboard backend (Epic #436 Phase 2, DEV-009): new Rust service `sentinel-dashboard-backend` (axum HTTPS `:8001` + WebTransport/QUIC `:4434`) running **parallel** to the Bun/Hono dashboard (`:8000`) for a phased cutover. **Wire codec** `topic+msgpack+zstd` (frame `[2B topic_len][topic][1B 0x01][4B compressed_len][zstd(msgpack)]`, byte-compatible with the noaide console client; UUIDs as 16 raw bytes). **WebTransport** endpoint pushes topic frames over server-initiated unidirectional streams (`hello` frame proves the codec end-to-end; full event-push delta is #432); reuses `sentinel-console-plane` (CAS) for #432. **Auth** = httpOnly `sentinel_session` cookie (port of `auth-session.ts`, #402/#405): constant-time key compare, 12h in-memory tokens, `require_auth` on control routes, **also enforced at the WebTransport handshake** (unauthenticated session rejected). **Projection reads** open `projection.db` read-only (`SQLITE_OPEN_READ_ONLY`, the #439-F3 lesson) and serve `agent_live_view`/`room_live_view`/`kpi_1m`/`task_kanban` as JSON. **Control-proxy** forwards to the Operator-API (`:8084`, `x-sentinel-operator-key`) and Gateway control plane (`:8081`). **TLS** = shared self-signed cert (rcgen, 13-day validity) for both HTTPS + WebTransport; `GET /api/cert-hash` exposes base64(sha-256(DER)) for the browser's `serverCertificateHashes` pinning (LAN-IP primary path like #439; a CA-issued cert over netbird/AD-CS is a follow-up). Serves the SolidJS bundle via `ServeDir` (placeholder `index.html`; the full bundle is #419). 10/10 tests green (codec roundtrip + byte-layout, auth lifecycle, read-only projection query, WebTransport roundtrip decode + auth-reject); workspace clippy `-D warnings` green.
- Added Issue #439 CAS console data-plane + WebTransport/QUIC push (Epic #436 Phase 1, DEV-009): new `sentinel-console-plane` crate reuses the `sentinel-fs` content-defined chunker + blake3-128 + zstd to content-address console data (conversations/telemetry/events) into a deduplicated, append-only block log; `delta(client_manifest)` returns only the blocks a client is missing (recurring system/company-context blocks dedup heavily) — replacing the dashboard's 1s full-state poll. New `sentinel-console` service is a WebTransport/QUIC (wtransport 0.6) push server: per session the client sends a `HelloManifest` (hashes it has) and the server replies with a `Delta` (length-prefixed JSON over a QUIC bi-stream); a background thread ingests the Limbo event store and logs live dedup/savings ratios. Verified: unit tests for dedup ratio + delta-only-missing (transfer ≪ full state, ~10% on a 90%-known manifest) and an in-memory protocol round-trip integration test.
- Added Issue #438 hierarchical Task/Auftrag entity (Epic #436 Phase 1): `TaskId`/`TaskStatus` + `TaskState` ECS component (assigned_to, assigned_by, parent_task for multi-level delegation, status, result) + 5 event-sourced `DomainEvent` variants (TaskCreated/Assigned/StatusChanged/Completed/Blocked). `OperatorTaskCommand` + `POST /operator/task` (create/assign/update-status/complete) → `handle_task_command` in `operator_command_system` spawns/updates task entities, emits events, and delivers new/assigned tasks to the agent via the existing **Voice-of-Gaia** path (no despawn/reset of agents, §6 L5). `task_progress_system` derives progress from agent actions (an agent active this tick advances its `Pending` tasks to `InProgress`). New `task_kanban` projection (table + handler) backs the console Kanban. Tasks are Time-Machine-capable (added to `EcsSnapshot`, snapshot/restore round-trips).
- Added Issue #437 `sentinel-ctl` operator CLI (Epic #436 Phase 1, CLI statt MCP / DEV-008): a Rust CLI Gaia (a local `claude -p`) calls via Bash. Subcommands (`chat-to-room`, `dm-agent`, `broadcast`, `voice-of-gaia`, `apply-config`, `restore`, `snapshot`, `nightrun`, `task <create|assign|status|complete>`, `gateway-reload`, `platform <analyze|reconcile|state|runtime-health>`, `observe <snapshots|runtime-health|platform-state|fs-stats>`) each map to a verified Operator-API (`127.0.0.1:8084`) or gateway control-plane path via `reqwest::blocking`, with the `x-sentinel-operator-key` header when configured. Every subcommand carries a **risk tag** (read/mutate/high-risk); mutating/high-risk actions pass through a **policy gate** (require `--confirm` or `SENTINEL_CTL_ASSUME_YES`; denial prints a clear error + exits non-zero **without sending a request**). `--json` gives machine-readable output for Gaia. Also adds the 1:1 `POST /operator/dm` backing path (`OperatorDmCommand` → directed perception with visible sender, `OperatorDmSent` audit event) so `dm-agent` maps to a real endpoint.
- Added Issue #440 Cortex-Gateway hot-reload for company-context + agent-DNA (Epic #436 Phase 1): the gateway control plane (port 8081) gains `POST /control/reload` (re-reads `company-context.md` and atomically swaps the cached value via a new mutex-guarded `Assembler.ReloadCompanyContext()`, plus `tomlLoader.InvalidateAll()` for agent DNA) and `POST /control/dna/invalidate` (`{agent_ids?}`; empty ⇒ all) — both make **no** provider/LLM call (token-safe). The daemon now fires a real best-effort DNA-invalidate to the gateway after each successful config-apply (#425), in a detached thread (`reqwest::blocking`, 2s timeout, no-op when the gateway is down), replacing #425's log-only trigger. **Fix:** `LoadCompanyContext` resolved the wrong path (`config/config/company-context.md` due to a doubled `config` join) so company-context never actually loaded on the deploy layout (`agents_dir=config/agents`); it now reads `<configDir>/company-context.md` (the agents' parent dir). Verified on the VM: changing the file + reload reports the new byte size, gateway logs the load, 0 provider calls.

- Added Issue #441 social/culture gaia-spec fields + deterministic company-context generation (Epic #436 Phase 1): `GaiaSpec` gains a `CultureSpec` (culture axes formality/collaboration/conflict_level/innovation/diversity in [0,1] + `mission` + `values[]`, all `serde(default)` so pre-existing specs stay valid). These axes deterministically shape the generated agents' Big-Five distribution via `score_with(center, spread)` — innovation→Openness center, formality→Conscientiousness center, collaboration→Agreeableness center, diversity→global spread, conflict_level→extra Neuroticism spread — staying blake3-reproducible (same spec+seed ⇒ identical values). `sentinel-gaia` now renders a full `company-context.md` deterministically (no LLM) from the spec (sections Unternehmen/Mission & Werte/Organigramm/Abteilungs-KPIs/Kultur; mission/values from the Gaia-filled spec or derived from `company_type` when empty) and emits it into the config tree; `validate_output_dir` asserts it exists and names the company, so a Gaia-generated company is self-standing (no PixelPerfekt default). The runtime `company.toml` guard is unchanged.
- Added Issue #425 daemon runtime config-apply (Epic #436 Phase 1): new operator endpoint `POST /operator/config/apply` (auth-gated, native daemon IPC later wrapped by `sentinel-ctl` #437) that applies a company config `{mode, agents[], building}` to the running ECS world without a reset. **Live mode** diffs the inline config against the currently-applied one by `identity.id` and applies only the delta — new agents spawn (current shift), changed agents live-update in place via `apply_personality`/`apply_identity`/`apply_capabilities` (NO despawn, so Memory/Bio/Evolution and unrelated agents stay untouched), removed agents despawn through the full teardown path; agents under active Control-Plane healing (`runtime_health` "stale") are deferred and retried (TOGAF §6 L3). **Fresh mode** tears down all agents and respawns the current shift from the new config. Both modes validate before any mutation (`max_agents` #414 bound, room adjacency+capacity, personality range, AgentId bounds, duplicate ids → 4xx with error list, no apply), take a pre-apply safety snapshot (rollback point), rebuild room maps, persist the applied config atomically back into `config_dir` (temp+rename + backup; daemon is the sole config_dir writer #420) so it survives a restart, emit a `ConfigApplied` domain event, and trigger Gateway DNA-cache invalidation (best-effort; gateway-side reload is #440, no-op if the gateway is inactive). The apply runs between ticks (after `schedule.run`) → tick-synchronous. An explicit user edit of base personality is a legitimate live change (the "don't overwrite evolution" guardrail applies only to Gaia-autonomous redb evolution, §6 L2). `AgentConfig`/`BuildingConfig` gained `Serialize` for the config_dir write-back.
- Added Issues #415/#416 Gaia company bootstrap: deterministic `sentinel-gaia` generator core plus CLI `init`/`preview`/`validate`/`print-example-spec`, persisted `gaia-spec.toml`, generated Agent TOMLs, `rooms.toml`, `daemon.toml`, `nightrun.toml`, overwrite protection with forced backups, JSON summaries, and daemon dry-run/start integration.
- Added Issue #411 runtime registry and workload routing checks for explicit `ecs-native`, `wasm-wasmtime`, and `bwrap-landlock` selection under the plural NanoRuntime contract.
- Added Issue #410 `bwrap-landlock` NanoRuntime adapter with documented config+filesystem snapshot semantics and no RAM/CRIU checkpoint claim.
- Added Issue #409 `wasm-wasmtime` NanoRuntime adapter without a bitwise Wasmtime Store dump claim. Its original input-bearing snapshot schema is retained for compatibility, but current restore binds only completed results and never re-executes stored input.
- Added Issue #408 shared `NanoRuntime` trait, runtime keys, registry primitives, conformance harness, and ECS-native reference implementation.
- Added Issue #407 DEV-007 contract check script for the Nano-Container runtime decision and Cluster 12 gap-document alignment.
- Added Issue #393 Kani verification baseline: installed/verified Kani+CBMC on the build server, added six proven harnesses across bio, snapshot cursor, and event-store offset/dedup invariants, and documented limits in `docs/security/kani-verification.md`.
- Added Issue #392 unsafe audit baseline: first-party unsafe inventory, local `SAFETY:` justifications, CI-ready baseline checker, and a safe `MaybeUninit` replacement for the nightrun `localtime_r` path with fixed-time shift regression coverage.
- Added Issue #383 component-level READMEs for 25 current Rust/Go component directories, plus `docs/component-readmes.md` and `scripts/check-component-readmes.sh` coverage verification.
- Added Issue #391 prompt-injection defense in the Cortex Gateway: per-agent tool capabilities from agent TOML, server-side action validation, `agent_action_rejected` audit events, and injection/legitimate-action regression coverage without real LLM calls.
- Added Issue #390 security threat model at `docs/security/threat-model.md`, covering compromised-agent, external-attacker, and supply-chain attacker classes with asset inventory and prioritized follow-up gaps.
- Recorded Issue #396 Cluster 12 runtime-contract decision as DEV-006 in the Deviation Register; DEV-006 is now a superseded historical record after #407/DEV-007.
- Implemented Issue #384 Time-Travel Debugging UI (TOGAF Cluster 11): new dashboard "Zeitreise" view with a visual snapshot timeline (tier badge, timestamp, tick, size), point-in-time world-state preview (active agents from `tick_snapshot`, present agents and per-room occupancy derived from EventStore lifecycle replay up to the snapshot boundary via new `GET /api/control/snapshot-state`), and a hot-swap Restore flow with confirmation dialog. Fixed the broken `snapshot_restored` WebSocket handler (`loadAgents`/`loadRooms` ReferenceError) so the dashboard live-refreshes after a restore. Verified end to end on the Deploy-VM with Playwright (all five ACs).
- Added `ebpf-mode-smoke` for focused kernel-mode/userspace-fallback runtime checks and committed Issue #380 dashboard screenshot evidence at `docs/screenshots/issue380-dashboard-ebpf.png`.
- Added dashboard exposure for Issue #276 benchmark evidence at `/api/metrics/benchmarks`, including hardware scope, relative-only comparison notes, and system-metric log labels.

## [0.1.0-alpha] - 2026-04-26

First public release.

This is the first **public** release. The project was developed privately
prior to this release. `v0.1.0-alpha` marks the boundary between private
development and public visibility — not the beginning of the project itself.

### Added
- Complete TOGAF v22.1 architecture guide in `docs/architecture/`
- `docs/governance.md` mapping governance mechanisms to code paths (3 controlplanes,
  policy-as-code locations, event-store as audit trail, sandbox boundaries,
  16 CI workflows)
- `docs/togaf-gap-v22.md` — per-cluster implementation status across 12 TOGAF v22.1 clusters
- `docs/togaf-deviations-v22.md` — five deliberate deviations (DEV-001..005) with
  what / why / revisit-when for each
- `docs/glossary.md` — PixelPerfekt narrative and 60-LLM + 5-services agent-layer terminology
- Run-able 10-minute demo via `docker-compose.demo.yml` (`make demo`) bringing up NATS +
  sentinel-daemon + cortex-gateway + sentinel-judge + sentinel-nats-bridge + sentinel-projection + dashboard
- 3-tier `make demo-binaries` recipe (release-fetch / cargo-remote / local cargo build) plus
  `scripts/fetch-demo-binaries.sh` so the demo onboard is ~60 MB instead of a 20-min cargo build
- Pre-built Linux x86_64 release artifacts: `sentinel-daemon`, `sentinel-nightrun`,
  `sentinel-projection` attached to the v0.1.0-alpha release
- Demo dashboard recording in `docs/images/sentinel-demo.gif`
- 4-tier IP / path configuration strategy: `.env.example`, `.make.local.example`,
  `deploy/systemd/sentinel-env.example` and `Makefile` `-include .make.local`
- CodeQL configuration `.github/codeql/codeql-config.yml` with explicit path scoping;
  Go uses `build-mode: manual`, dashboard uses `build-mode: none`

### Changed
- Go module path migrated from `github.com/obtFusi/project-sentinel` to
  `github.com/silentspike/project-sentinel` (4 modules, 38 source files)
- README restructured around the runtime-and-sandbox research positioning and the
  60 LLM-persona + 5 background-services framing
- `llms.txt` rewritten with the same agent-layer framing and explicit cross-links
  into the new architecture docs

### Security
- Pre-release security review:
  - `gitleaks` scan: 0 leaks across 1063 commits / 12.58 MB
  - `trufflehog` scan: 0 verified + 0 unverified secrets across 56851 chunks / 521.76 MB
  - `cargo audit`, `govulncheck`, `npm audit`: clean
  - 9/9 sandbox breakout tests passing (bwrap + Landlock + cgroups + netns); see
    [docs/security-test-report.md](docs/security-test-report.md)
- CI workflow status at release: ci, lint, coverage, supply-chain (cargo-deny, npm-audit,
  go-vuln, rust-audit), conventional-commits, dependency-freshness — green on main.
  CodeQL scanning depends on GitHub Advanced Security, which is automatically enabled
  for public repositories; the workflow files (`.github/codeql/codeql-config.yml`,
  `.github/workflows/codeql.yml`) ship configured so the SAST pipeline goes green
  on the first scheduled run after the public-flip.

### Known limitations at this release
- The docker compose demo intentionally exercises only the deterministic + LLM layers
  (ECS world, bio-engine, gateway pipeline, dashboard). It does **not** exercise
  the kernel-bound sandbox primitives (bwrap, Landlock, cgroups v2, netns, eBPF,
  sentinel-fs FUSE) — those need user namespaces and CAP_BPF / CAP_SYS_ADMIN that
  a plain unprivileged container does not have. The full stack is documented in
  `deploy/systemd/*.service` for VM deployment. See README "What the docker demo
  shows — and what it does not" and [docs/known-limitations.md](docs/known-limitations.md).
- The signed v0.1.0-alpha tag will display "Unverified" in the GitHub UI until the
  maintainer's SSH signing key is registered as a Signing Key on GitHub. The tag
  itself carries a valid Ed25519 signature (verifiable locally with
  `git tag -v v0.1.0-alpha`).

[Unreleased]: https://github.com/silentspike/project-sentinel/compare/v0.1.0-alpha...HEAD
[0.1.0-alpha]: https://github.com/silentspike/project-sentinel/releases/tag/v0.1.0-alpha
